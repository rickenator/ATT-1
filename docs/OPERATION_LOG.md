# Operation Log

## Current Objective

Build ATT-1: a C11 programmable tensor-tile simulator for clustered LLM inference, using binary `.att1` model artifacts, CPU reference execution, CUDA live-model validation later, and ATT-1 custom PCIe silicon as the future Phase 3 hardware target.

## Current Milestone

Milestone 110: minimal PCIe/AIMU prototype design review (complete).

## Active Task

Prepare Milestone 111.

## Hard Rules

- Runtime is C11.
- Tests are C11.
- Python may generate artifacts under `compiler/`, but must not be required by `make test`.
- `.att1` format must remain versioned and validated.
- Loader must treat binary files as hostile input.
- Default build must not require CUDA.
- CUDA must be opt-in, for example `make CUDA=1`.
- CPU f32 backend remains the correctness reference.
- Do not add Vulkan, Android, OpenCL, or mobile targets.
- Do not implement full CUDA transformer inference until the CUDA skeleton is clean.
- Keep milestones narrow and commit-ready.

## Completed Milestones

- Milestone 0: project skeleton, build, smoke test, and baseline docs.
- Milestone 1: tensor/runtime core object lifetimes and structured status handling.
- Milestone 2: float32 tensor primitives and local transformer math building blocks.
- Milestone 3: paged KV cache and KV-MMU simulator with append validation.
- Milestone 4: packet fabric simulator with bounded queues, barriers, broadcast, and counters.
- Milestone 5: tile runtime threads, command dispatch, and lifecycle coverage.
- Milestone 6: versioned `.att1` model format, hostile-input loader checks, inspect tool, and dummy model generator.
- Milestone 7: single-tile tiny LLM inference from loaded `.att1` model artifacts.
- Milestone 8: multi-tile sharding and cluster inference path.
- Milestone 9: tracing, benchmark tools, and synthetic sizing/reporting flow.
- Milestone 10: q8 quantization primitives and q8xf32 matmul support.
- Milestone 11: backend abstraction with CPU f32 and CPU q8 backends wired through inference.
- Milestone 12: CUDA backend skeleton — opt-in CUDA build path, lifecycle hooks, copy helpers, and stub operator vtable.
- Milestone 13: CUDA validation plan — `docs/CUDA_VALIDATION_PLAN.md` and environment-aware bench smoke test.
- Milestone 14: CUDA f32 matmul prototype — `cublasSgemm` wired into `cuda_backend_matmul_f32`; five-case validation suite.
- Milestone 15: CUDA RMSNorm prototype — `cuda_backend_rmsnorm_f32` with cuBLAS primitives; validated on CPU-only and CUDA paths.
- Milestone 16: CUDA FFN/SwiGLU prototype — `cuda_backend_ffn_swiglu_f32` validated against CPU f32 reference.
- Milestone 17: CUDA RoPE prototype — `cuda_backend_rope_f32` with per-pair `cublasSrot`; validated against CPU f32 reference.
- Milestone 18: CUDA causal attention — `cuda_backend_softmax_f32`; attention forward uses all CUDA operators; 7-case test suite.
- Milestone 19: CUDA transformer block prototype — single-block backend path validated using composed CUDA primitives vs CPU f32.
- Milestone 20: CUDA single-tile inference integration — single-tile decode validated with CUDA backend; CPU-vs-CUDA equivalence on dummy model.
- Milestone 22: CUDA cluster inference — `run_cluster()` validated on CUDA backend; bench exits zero with `backend=cuda`; fabric counters active.
- Milestone 23: CUDA q8xf32 matmul — CUDA q8xf32 matmul validated against CPU q8xf32 reference; CPU-only build remains CUDA-free.
- Milestone 27: CPU q8 cluster inference — validated against CPU f32 cluster reference; logits tolerance 0.15 enforced; 38 tests pass.
- Milestone 28: CUDA q8 cluster inference enabled — removed cpu-only rejection from `run_cluster()`; cuda-q8 cluster path validated; `test_cuda_q8_cluster` and `test_q8_bench` added.
- Milestone 29: backend matrix regression harness — `tests/test_backend_matrix.c` covers all 8 (backend × mode) combos against the dummy model.
- Milestone 30: real model conversion plan — `docs/real_model_conversion.md` added; tensor naming, dtypes, matrix conventions, q8 rules, and validation ladder documented.
- Milestone 31: ATT-1 converter skeleton — `compiler/convert_llama_to_att1.py` validates LLaMA `config.json` and prints planned ATT-1 config; `compiler/fixtures/tiny_llama/` fixture added.
- Milestone 32: deterministic converter stub output — binary `.att1` emitter; tensor names match `src/model_view.c`; determinism confirmed by SHA256.
- Milestone 33: AIMU tiled tensor architecture document — `docs/aimu_architecture.md` added; `README.md` updated with AIMU fabric paragraph.
- Milestone 34: ATT-1 shard metadata design — `docs/shard_metadata.md` added; 13 fields, fixed 120-byte record layout, versioning and validation rules.
- Milestone 35: optional shard metadata C parser skeleton — `include/att1_shard_meta.h`, `src/shard_meta.c`, model loader overlap check; `tests/test_shard_meta.c` (8 cases).
- Milestone 36: deterministic shard metadata fixture — `compiler/make_shard_meta_fixture.py`; `models/shard_meta/model.att1` checked in; `att1-inspect` per-record output; 4 fixture tests.
- Milestone 37: shard metadata reporting and trace integration — `att1_shard_meta_summarize()`; `att1-inspect` summary header; `att1-bench` `shard_meta=present/absent`; 4 report tests.
- Milestone 38: shard metadata consistency validation — `att1_shard_meta_validate()`; `att1-inspect` violations block; 8 consistency tests.
- Milestone 39: metadata-driven shard plan proposal — `att1_meta_plan_build()`, `att1_meta_plan_compare()`; `att1-inspect` plan block; 5 plan tests. Advisory only.
- Milestone 40: opt-in metadata shard plan execution — `att1_shard_plan_mode` enum, `att1_shard_plan_from_meta()`; `--shard-plan runtime|metadata` CLI flag; `shard_plan=` output; 8 exec tests. No silent fallback.
- Milestone 41: backend matrix validation for shard plans — `tests/test_backend_matrix.c` extended to 16 entries (4 groups); 3 consistency cross-validation groups.
- Milestone 42: converter stub with shard metadata — `--tiles N` and `--shard-meta` converter flags; `models/converted_stub_meta/model.att1` (2-tile, 21 records) checked in; backend matrix extended to 24 entries.
- Milestone 43: converter shard metadata plan report — `--report` and `--report-json` converter flags; `build_shard_plan_report()` added; Python-skippable smoke test; 38 tests pass.
- Milestone 44: converter executable metadata plan validation — `tests/test_converter_validation.c` added (inspect + bench consistency, no Python at test time); `compiler/validate_converter_flow.sh` added.
- Milestone 45: real tiny model import plan — `docs/real_tiny_model_import.md` added (tensor mappings, transpose rules, RoPE, dtype conversion, validation ladder, M46–M50 split).
- Milestone 46: safetensors metadata scanner — `compiler/scan_safetensors.py` added; `compiler/fixtures/tiny_llama_2l.safetensors` (21 tensors) checked in; Python-skippable smoke test.
- Milestone 47: safetensors tensor reader — `compiler/load_safetensors.py` added; `compiler/test_load_safetensors.py` (10 error-case checks); tensor reader smoke test.
- Milestone 48: f32 ATT-1 conversion from tiny safetensors — `--safetensors PATH` converter flag; `models/real_tiny_f32/model.att1` checked in; converter validation test updated.
- Milestone 49: q8 ATT-1 conversion from tiny safetensors — `--weight-format q8` converter flag; `models/real_tiny_q8/model.att1` checked in; CUDA=1 backend matrix 24/24.
- Milestone 50: tokenizer import plan — documentation-only plan in `docs/real_tiny_model_import.md` and `docs/real_model_conversion.md`; M51–M55 split defined.
- Milestone 51: tokenizer metadata schema — `docs/tokenizer_metadata.md` added; schema version 1 covering tokenizer type, vocab_size, special token IDs, normalization, asset hash, runtime fallback rules.
- Milestone 52: tokenizer asset scanner — `compiler/scan_tokenizer.py` added; `compiler/fixtures/tiny_tokenizer/` (16-token BPE) added; Python-skippable smoke test.
- Milestone 53: tokenizer fixture import report — `build_import_report()`, `--report`, `--report-json`, `--model-config` added to `scan_tokenizer.py`; import report smoke test.
- Milestone 54: optional `.att1` tokenizer metadata section — `include/att1_tok_meta.h`, `src/tok_meta.c`; `.att1` version 2 defined (96-byte header); `models/tok_meta/model.att1` checked in; `tests/test_tok_meta.c` (8 cases).
- Milestone 55: runtime tokenizer selection plan — documentation-only; three modes defined (`byte`, `metadata`, `external`); M56–M60 split; hard-error policy for unsupported tok_meta.
- Milestone 56: tokenizer selection CLI stub — `--tokenizer byte|metadata|external` added to `att1-bench`; `tokenizer=<name>` in bench output; 6 selection smoke tests.
- Milestone 57: metadata tokenizer validation path — `att1_tok_meta_check_runtime()` added; differentiated error messages in `check_tokenizer_mode()`; `tests/test_tok_meta_select.c` (15 cases).
- Milestone 58: external tokenizer preprocessing mode — `include/att1_tok_ext.h`, `src/tok_ext.c`; `--input-token-ids` and `--tokens-file` CLI flags; `tokenizer=external` bench output; `tests/test_tok_ext.c` (15 cases).
- Milestone 59: local Hugging Face tokenizer helper — `compiler/tokenize_hf.py` added; outputs comma-separated IDs for `att1-bench --input-token-ids`; HF-package-skippable smoke test.
- Milestone 60: converted model validation with pretokenized input — `test_converter_validation.c` extended with external-tokenizer bench round-trip; pretokenized pipeline smoke test.
- Milestone 61: source-model comparison harness — `compiler/read_att1.py` and `compiler/compare_att1_to_source.py` added; `models/m61_f32/` and `models/m61_q8/` checked in; f32 max_abs_error=0, q8 max_abs_error<0.6.
- Milestone 62: source comparison report integration — `--report`, `--report-json`, `--tokens-file`, `--backend` flags added to `compare_att1_to_source.py`; structured `rpt` dict; M62 smoke sub-tests.
- Milestone 63: larger tiny-model fixture — `compiler/fixtures/m63_llama_2l.safetensors` (vocab=64, d_model=32); `models/m63_f32/` and `models/m63_q8/` checked in; all four bench modes validated.
- Milestone 64: public small-model import candidate selection — `HuggingFaceTB/SmolLM2-135M` recommended; BF16 blocker and GQA requirements documented; M65–M69 prereq plan defined.
- Milestone 65: public model acquisition and import instructions — SmolLM2-135M download options, directory layout, preflight validation commands, and failure triage documented.
- Milestone 66: public model compatibility scanner — `compiler/check_llama_compat.py` added; `compiler/fixtures/m66_compat_fixture/` added; compat report smoke test (4 checks).
- Milestone 67: BF16/F16 source dtype coercion — `_coerce_bf16()` and `_coerce_f16()` added to `load_safetensors.py`; `compiler/fixtures/m67_bf16_llama_2l.safetensors` checked in; BF16 coercion smoke test; 42 tests pass.
- Milestone 68: q8 conversion of BF16-source public model — `compare_att1_to_source.py` extended to coerce BF16/F16 source tensors; q8 public-model workflow in `docs/quantization.md`; q8 conversion smoke test (5 checks).
- Milestone 69: public model source comparison report — `compare_att1_to_source.py` accepts `--model-dir`; GQA-shaped K/V tensors supported in Python reference; source comparison report documented.
- Milestone 70: public model backend smoke validation — `compiler/validate_public_backends.py` added; CPU f32/q8 single and cluster paths validated; CUDA rows report `unsupported`; no public artifacts committed.
- Milestone 71: public model end-to-end tokenized validation — `compiler/validate_public_tokenized.py` added; `tokenize_hf.py` + pretokenized bench path combined; HF-package-skippable smoke test.
- Milestone 72: larger-model scaling and placement report — `tools/att1-size.c` extended with `--config`, manual shape, `--json` modes; per-category storage breakdown, KV-cache table, AIMU tile placement, backend feasibility notes; scaling report smoke test (6 checks).
- Milestone 73: q4 quantization planning — documentation-only; grouped int4 format spec in `docs/quantization.md`; M74–M79 implementation split defined.
- Milestone 74: q4 format and schema — `ATT1_MODEL_DTYPE_Q4=3` added; q4 wire-format constants; hostile-input validation in loader; q4 tensor reporting in `att1-inspect`; `tests/test_quant_q4.c` (9 cases).
- Milestone 75: CPU q4 packing and unpacking primitives — five per-group helpers in `src/quant.c`; nibble convention, signed int4 [-7,7], saturation, non-finite rejection; `tests/test_quant_q4_pack.c` (9 cases).
- Milestone 76: CPU q4 matmul prototype — `att1_q4_matrix` struct, `att1_quantize_q4_per_group()`, `att1_matmul_q4xf32()`; dequantize-then-multiply; Q4_TOLERANCE=0.35f; `tests/test_matmul_q4.c` (8 cases).
- Milestone 77: q4 fixture generation and validation — `compiler/make_q4_fixture.py`; `models/q4_tiny/model.att1` checked in; `att1-inspect` `inference_status=q4_unsupported`; `tests/test_quant_q4_fixture.c` (8 cases).
- Milestone 78: CPU q4 single-tile inference integration — `att1_infer_create_q4()`, `cpu-q4` backend, zero-copy q4 weight views, q4 attention and transformer block forward; `tests/test_infer_q4.c` (7 cases).
- Milestone 79: CPU q4 cluster inference integration — `att1_cluster_infer_create_q4()`; `cluster_prepare_q4()`; `use_q4` branch in decode loop; metadata plan returns `ATT1_ERR_UNSUPPORTED`; `tests/test_cluster_infer_q4.c` (8 cases).
- Milestone 80: q4 bench and backend matrix integration — `att1-bench --backend cpu-q4` single and cluster validated; `--backend cuda-q4` hard-rejected; `test_q4_bench` (4 checks); backend matrix extended with cpu-q4 consistency group.
- Milestone 81: public-model q4 converter — `compiler/convert_llama_to_att1.py` extended with `--weight-format q4`; `models/real_tiny_q4/model.att1` checked in; `test_converter_validation.c` extended with q4 round-trip.
- Milestone 82: public-model q4 conversion plan — documentation-only; 211 eligible q4 tensors for SmolLM2-135M; size estimates; tolerance targets; M83–M85 split defined.
- Milestone 83: public-model q4 conversion path — `--model-dir` auto-discovery in converter; `compiler/validate_public_q4.py` added; `compiler/fixtures/m83_model_dir/` fixture; q4 public model smoke test.
- Milestone 84: public-model q4 source comparison report — `--att1-q4` and `--q4-tol` flags in `compare_att1_to_source.py`; q4 decode support in `compiler/read_att1.py`; q4 source comparison smoke test.
- Milestone 85: public-model q4 backend smoke validation — `compiler/validate_public_q4_backends.py` added; cpu-q4 single/cluster validated; cuda-q4 explicitly unsupported; q4 backend smoke test (3 parts).
- Milestone 86: CUDA q4 implementation plan — documentation-only; Option A/B/C design choices in `docs/quantization.md`; M87–M90 milestone split in `docs/cuda_backend.md`; no C or Makefile change.
- Milestone 87: CUDA q4 matmul prototype — `cuda_backend_matmul_q4xf32()` (dequantize-on-CPU then cuBLAS); `cuda_q4_backend_ops` ("cuda-q4"); `att1_backend_cuda_q4_create()`; `matmul_q4xf32` slot in `att1_backend_ops`; all CPU backends updated to NULL; `tests/test_cuda_matmul_q4.c` (8 cases); 46 tests pass.
- Milestone 88: CUDA q4 single-tile inference — `cuda_q4_backend_ops` populated with full inference ops (rmsnorm, softmax, rope, ffn_swiglu); `att1_attention_forward_backend_q4` and `att1_transformer_block_forward_backend_q4` route matmuls through backend vtable via `attention_q4_matmul`/`block_q4_matmul` helpers; `infer_backend_is_q4` accepts "cuda-q4"; output projection in `att1_infer_decode_token` routed via `infer_matmul_q4`; `att1-bench` cuda-q4 single mode enabled (cluster remains rejected); `test_q4_bench` updated for CUDA-conditional cuda-q4 check; `tests/test_cuda_infer_q4.c` (3 cases: no-fallback, logits match, generated tokens); 48 tests pass.
- Milestone 89: CUDA q4 cluster inference — `cluster_backend_is_q4()` extended to accept "cuda-q4"; `cluster_matmul_q4()` static helper routes output projection through backend vtable when supported; cuda-q4 cluster rejection removed from `att1-bench` `run_cluster`/`run_cluster_external`; cuda-q4 backend swap wired after `att1_cluster_infer_create_q4`; `tests/test_cuda_cluster_infer_q4.c` (4 cases: no-fallback, fabric counters, logits match, generated tokens); `test_q4_bench` extended with cuda-q4 cluster status check; 53 tests pass (7 CUDA-skipped on CPU-only host).
- Milestone 90: CUDA q4 benchmark and backend-matrix integration — `test_backend_matrix` extended with cuda-q4 single and cluster runtime entries (group 4, q4_tiny fixture, CUDA-conditional); `validate_public_q4_backends.py` M85 smoke assertion relaxed from `status=unsupported` to `result: pass`; 28-entry matrix; M85 CUDA q4 policy note updated; CPU-only host: 14/28 passed, 14 skipped, 0 failed; CUDA host: 28/28 passed, 0 skipped, 0 failed (verified).
- Milestone 91: CUDA q4 public-model validation report — `compiler/validate_public_q4_cuda.py` added; 5-case validation matrix (cpu-q4 × single/cluster/metadata, cuda-q4 × single/cluster); plan_unsupported and unavailable are non-failure outcomes; backend silent-fallback check; fabric-packet nonzero check; q4 notes embedded; JSON report; `check_public_q4_cuda_smoke` added to `test_bench_smoke.c`; CPU-only: bench smoke passes; CUDA host: all cuda-q4 rows pass (verified).
- Milestone 92: Backend comparison report — `compiler/backend_comparison_report.py` added; 12-case matrix (f32/q8/q4 × CPU/CUDA × single/cluster); pending/unavailable/pass CUDA status; backend silent-fallback check; fabric-packet nonzero check; q4 notes; JSON report; `check_backend_comparison_smoke` added to `test_bench_smoke.c`; CPU-only: all 6 CPU rows pass, 6 CUDA rows pending (no --include-cuda); CUDA host: all 12 rows pass (verified on RTX 3090).
- Milestone 93: AIMU/PCIe prototype requirements — Section 8 added to `docs/aimu_architecture.md`; covers prototype goal, proof criteria, runtime/CUDA relationships, tile responsibilities, local memory sizing (f32/q8/q4), host control plane, fabric/interconnect requirements, KV-MMU requirements, counter/trace requirements, four MVP options (software-emulated/FPGA/PCIe card/ASIC), data movement assumptions, dtype tolerances, non-goals, open engineering questions, and M94 proposal.
- Milestone 94: ATT-1 trace diff tool — `compiler/trace_diff.py` added; parses att1-bench key=value output; compares all scalar, layer[N], and tile[N] fields; same/DIFF/MISSING tags; JSON report via --report-json; `check_trace_diff_smoke` added to `test_bench_smoke.c` (identical diff, cross-backend DIFF, malformed rejection); `docs/trace_diff.md` added; no C/Makefile/format changes.
- Milestone 95: Prefill vs decode benchmark split — `att1-bench` now snapshots trace counters at the prefill/decode boundary and emits: `prompt_tokens` (byte and external modes), `decode_tokens`, `prefill_time_us_total`, `decode_time_us_total`, `prefill_kv_appends`, `decode_kv_appends`, `prefill_kv_reads`, `decode_kv_reads`, `prefill_logits_bytes`, `decode_logits_bytes`; `prefill_fabric_packets` + `decode_fabric_packets` for cluster mode; all four runner functions updated; `check_prefill_decode_split_smoke` added to `test_bench_smoke.c`; `trace_diff.py` updated with new field names; `docs/tracing.md` updated; no inference behavior changed.
- Milestone 96: Tile memory capacity and bandwidth estimator — `tools/att1-size.c` extended with `--tile-memory-mib N`, `--tile-memory-gib N`, `--sessions N`, `--target-tokens-per-sec N`, `--fabric-gib-sec N` options; `capacity_opts` struct and `tile_capacity_status()`/`fabric_bandwidth_status()` helpers added; preset mode emits `tile_memory_mib`, `model_bytes_per_tile`, `tile_capacity_status`, and `fabric_bandwidth_status` lines; full/config/manual mode emits `[tile_capacity_estimate]` and `[fabric_bandwidth_estimate]` sections with combined model+KV utilization, PASS/WARN/FAIL/UNKNOWN status; JSON mode extended with `tile_capacity_estimate` and `fabric_bandwidth_estimate` objects; `check_tile_capacity_smoke` added to `test_bench_smoke.c` (6 scenarios); no C ABI, format, or inference behavior changed.
- Milestone 97: Metadata-driven tensor-level placement plan — documentation/spec only; `docs/shard_metadata.md` §13 added: tensor-level ownership record schema (200-byte extended record, tensor_category/slice_axis/slice_start/slice_end fields), slicing policies (row-wise, column-wise, head-wise, layer-wise, vocab/lm_head split, embedding split, KV-cache split, replicated norms), execution consequences (matmul, attention, FFN/SwiGLU, lm_head logits, reductions, fabric traffic, synchronization, trace determinism, error/tolerance), placement validation rules (12 checks), estimator integration plan, execution modes (advisory/validation/opt-in/enforced), non-goals, and M98–M103 future milestone split; `docs/aimu_architecture.md` §9 added: AIMU fabric implications of each split policy (broadcast to slice owners, partial result collection, head-local KV traffic, embedding lookup routing, logit concat routing), AIMU local memory layout for tensor slices, activation routing protocol DAG changes, trace determinism under tensor-level placement, estimator integration (AIMU perspective), prototype engineering path M98–M103, and AIMU-scope non-goals; no C, Makefile, binary format, or inference behavior changed.
- Milestone 98: Tensor-level placement report schema — documentation/spec only; `docs/tensor_placement_report.md` added: canonical schema for placement reports covering report header (§1, 15 fields, key-value and JSON formats), tile/AIMU summary records (§2, per-tile model_bytes/kv_bytes/activation_bytes/logits_bytes/fabric_payload/fabric_packets/utilization/capacity_status/bandwidth_status, PASS/WARN/FAIL thresholds), tensor placement records (§3, 13 tensor categories, 3 replication policies, 4 reduction behaviors, 5 routing_requirements values, 5 placement_status values, key-value and JSON formats), validation report fields (§4, 10 violation counters, severity levels, per-violation tensor_id/rule/message), human-readable output (§5, tile summary table, overloaded-tile report, largest-tensor report, warnings, remediation suggestions), JSON schema summary (§6, stability rules), relationship to existing tools (§7: M96 att1-size heuristic→exact mapping, shard metadata field mapping, M99 validator flow, M100 estimator integration); `docs/shard_metadata.md` §14 added: shard-metadata-to-report field mapping table, §13.5 validation rule→report violation mapping, report generation path diagram, non-goals; no C, Makefile, binary format, or inference behavior changed.
- Milestone 99: Tensor-level placement report validator prototype — `compiler/validate_tensor_placement_report.py` added: validates M98 placement report JSON; checks report header (report_version, dtype, tile_count, context/sessions/capacity/bandwidth targets), tile records (unique tile IDs in range, non-negative byte counts, capacity_status/bandwidth_status consistency, PASS-with-overflow detection), tensor records (owner_tile in range and in tiles list, slice axis/range validity, slice_end ≤ source_shape dimension, q4 group-size alignment rule 6, duplicate unique-policy detection rule 2, replication/reduction/routing field validation), coverage checks (overlapping slices rule 2, gap detection rule 3, reduction_behavior absent for split tensors rule 8); exit 0/1/2; --report/--report-json/--strict CLI; 5 checked-in fixtures (valid, invalid_tile_id, capacity_overflow, overlapping_slices, q4_bad_align); `check_placement_validator_smoke()` added to `tests/test_bench_smoke.c` (8 scenarios: valid pass, invalid tile fail, capacity overflow fail, overlapping slices fail, q4 alignment fail, malformed JSON exit 2, missing report_version fail, JSON output keys present); `docs/tensor_placement_report.md` §9 added: validator usage, exit codes, checks table, fixture table, JSON output format; no C ABI, binary format, or inference behavior changed.
- Milestone 100: att1-size tensor placement report emission — `tools/att1-size.c` extended with `--placement-report-json PATH`; new `write_placement_report_json()` emits M98-schema report JSON using layer-wise placement estimates; helper functions `m100_dtype()` (maps f16→f32), `m100_qfamily()`, `m100_packed()`, `m100_scale()`, `m100_tile_model_bytes()`, `m100_emit_tensor()`; header: dtype, quantization_family, tile_count, context_length, sessions, tile_memory_mib, target_tokens_per_sec, fabric_gib_sec if provided; tiles array: per-tile model_bytes (dtype-aware), kv_bytes (f32/session), activation/logits/fabric bytes, capacity_status/bandwidth_status PASS/WARN/FAIL/UNKNOWN; tensors array: tok_embeddings, per-layer wq/wk/wv/wo/w_gate/w_up/w_down/attn_norm/ffn_norm, output_norm, lm_head (all unique, no slicing, slice_end=dim0 so q4 alignment always satisfied); failures[]/warnings[] with tile overflow/bandwidth issues; synthetic model warning emitted; `check_placement_report_smoke()` added to `tests/test_bench_smoke.c` (9 scenarios); `docs/tensor_placement_report.md` §10 added; no C ABI, binary format, or inference behavior changed.
- Milestone 101: advisory tensor-level placement proposal tool — `compiler/propose_tensor_placement.py` added: reads M98/M100 placement report JSON and produces advisory remediation report; detects capacity FAIL/WARN, bandwidth FAIL/WARN, KV pressure, logits traffic (gated on bandwidth constraint), load imbalance, oversized tensors, q4 alignment warnings, synthetic model notes; proposes required minimum tile count (`ceil(total_model_bytes / (tile_mem_bytes × 0.8))`), required tile memory, dtype conversion (f32→q8/q4, q8→q4), lm_head split, tok_embeddings split, layer redistribution, context/session reduction, fabric bandwidth increase, head-wise placement; advisory status ok/warn/fail; exit 0=ok/warn, 1=fail, 2=parse error; `--report-json PATH` optional JSON output with status/proposal_count/next_action/analysis/proposals; `check_placement_proposal_smoke()` added to `tests/test_bench_smoke.c` (8 scenarios: valid ok, gpt-oss 16 GiB FAIL generation, proposer detects capacity-fail, tile recommendation present, JSON output keys, JSON fail case fields, malformed JSON exit 2, missing tiles exit 2); `docs/tensor_placement_report.md` §11 added; no C ABI, Makefile, binary format, or inference behavior changed; make test: 27 PASS, 0 FAIL.
- Milestone 102: tensor placement scenario proposal generator — `compiler/propose_tensor_scenarios.py` added: reads M98/M100 placement report JSON and generates candidate placement/capacity scenarios for AIMU tile planning; cross-product of tile memory sizes (default 16/32/64/128 GiB), tile counts (heuristic defaults based on model bytes), context lengths, session counts, and fabric bandwidth targets; per-scenario metrics: model_bytes_per_tile (layer-wise split), kv_bytes_per_tile (scaled from report by context/sessions ratio), utilization_percent, capacity_status PASS/WARN/FAIL, estimated_fabric_gib_sec (linear approximation at target TPS), fabric_status PASS/WARN/FAIL/UNKN, recommendation_score; scenarios ranked by score (PASS first, fewer tiles, smaller memory, lower KV pressure); human-readable comparison table (up to 30 rows) with recommended row marked; SKU notes on 16/32/64 GiB trade-offs; CLI: `--report`, `--tile-memory-gib`, `--tile-count`, `--context`, `--sessions`, `--target-tokens-per-sec`, `--fabric-gib-sec`, `--report-json`; exit 0=PASS found, 1=no PASS scenario, 2=parse error; `check_placement_scenarios_smoke()` added to `tests/test_bench_smoke.c` (8 scenarios: tiny-dummy PASS, gpt-oss 16 GiB report generation, gpt-oss larger-SKU PASS present, comma-separated parsing, invalid tile memory, invalid tile count, malformed report, JSON keys); `docs/tensor_placement_report.md` §12 added; no C ABI, Makefile, binary format, or inference behavior changed.
- Milestone 103: AIMU/PCIe command packet requirements — documentation only; (see OPERATION_LOG for full entry)
- Milestone 104: AIMU/PCIe MMIO register map sketch — documentation only; `docs/aimu_register_map.md` added (13 sections + 2 appendices, ~430 lines): §1 register map assumptions (32-bit register width, little-endian, 4-byte alignment, RO/RW/RW1C/WO access types, reserved-bit policy, versioned map, per-device vs per-tile BAR0 layout with 64 KiB address map); §2 global device registers (16 registers at 0x0000–0x003F: DEVICE_ID vendor_id/device_class, DEVICE_VERSION major.minor.patch.build, REGISTER_MAP_VERSION, FEATURE_FLAGS_LOW/HIGH with 11 CAP_* bits, TILE_COUNT, COMMAND_QUEUE_COUNT, INTERRUPT_STATUS/ENABLE with 8 IRQ sources, GLOBAL_STATUS, GLOBAL_CONTROL self-clearing write-one triggers, RESET_CONTROL, ERROR_STATUS RW1C with 7 error bits, ERROR_DETAIL tile/cmd/session/code, TRACE_CONTROL tile_mask/level/trigger, COUNTER_SNAPSHOT_CONTROL); §3 per-tile register windows (17 registers per tile at 0x8000+N×0x800: TILE_ID, TILE_STATUS state/session_count, TILE_FEATURE_FLAGS with 4 per-tile CAP_LOCAL_* bits, TILE_MEMORY_CAPACITY/USED LOW/HIGH 64-bit pairs, TILE_KV_CAPACITY/USED LOW/HIGH, SUPPORTED_DTYPES f32/q8/q4, SUPPORTED_OPS_LOW 12 op bits, SUPPORTED_OPS_HIGH, TILE_FABRIC_LINK_MASK, TILE_ERROR_STATUS command_id/cmd_type/error_code RW1C, TILE_RESET_CONTROL 4 reset modes WO); §4 command queue registers (12 registers at 0x1000: CQ_BASE_ADDR LOW/HIGH, CQ_SIZE, CQ_HEAD RO, CQ_TAIL RW, CQ_DOORBELL WO new-tail notify, CQ_STATUS queue_state, CQ_ERROR RW1C error_slot, CQ_FENCE_VALUE poll for cross-tile fence completion, CQ_COMPLETION_ADDR LOW/HIGH, CQ_COMPLETION_SIZE); §5 DMA/buffer descriptor registers (64-byte descriptor layout: host_addr/device_addr LOW/HIGH, byte_length, stride, dim0/dim1, dtype, quant_group_size, flags with 6 bits, checksum/tensor_id/command_id; DMA_CONTROL/STATUS/ERROR_STATUS, descriptor ring BASE LOW/HIGH + SIZE); §6 fabric/interconnect registers (11 registers at 0x3000: FABRIC_STATUS active_tile_mask/state/link_count, FABRIC_CONTROL enable/pause/reset/backpressure, FABRIC_ROUTE_TABLE_BASE LOW/HIGH + SIZE, FABRIC_PACKET_COUNTER LOW/HIGH, FABRIC_PAYLOAD_BYTES LOW/HIGH, FABRIC_CONGESTION_COUNTER, FABRIC_ERROR_STATUS 5 error bits RW1C); §7 counter registers (24 64-bit counters at 0x4000–0x40B4: commands_issued/completed, local_ops, matmul/rmsnorm/rope/attention/ffn, kv_appends/reads, tensor_bytes_read/written, activation_bytes_sent/received, logits_bytes, fabric_packets_sent/received, stall_fence/dma/fabric/barrier/queue_full, errors_total; full summary table with ATT-1 equivalents); §8 trace/debug registers (7 registers at 0x5000: TRACE_BUFFER_BASE LOW/HIGH, TRACE_BUFFER_SIZE, TRACE_WRITE_PTR RO, TRACE_FLAGS capture_level/tile_mask, TRACE_DROPPED_EVENTS, TRACE_SNAPSHOT_CONTROL trigger/SNAPSHOT_NOW); §9 error/status code model (22 error codes 0x00–0xFF in 8 severity families: OK/BUSY/PENDING, invalid-command family 0x10–0x14, resource errors 0x20–0x22, fabric errors 0x30–0x32, data integrity 0x40–0x42, ordering 0x50–0x51, capability mismatch 0x60–0x61, fatal 0xF0–0xFF with recovery action table); §10 11-step host boot/probe sequence (read DEVICE_ID→verify register map version→read FEATURE_FLAGS→enumerate tiles + capacities→allocate command/completion/trace rings→configure CQ_BASE/SIZE/COMPLETION registers→configure TRACE_BUFFER registers→enable MSI-X→write GLOBAL_CONTROL.ENABLE_DEVICE→RESET_TILE_FULL all tiles→dispatch LOAD_TENSOR_TILE per placement report tensor); §11 relationship table to ATT-1 simulator (14 mappings: att1-size output→TILE_MEMORY_CAPACITY, att1_shard_t→LOAD_TENSOR_TILE, att1_cluster_infer_t→CQ_TAIL/DOORBELL, fabric_packets_sent→FABRIC_PACKET_COUNTER, kv_appends→CNT_KV_APPENDS, decode_logits_bytes→CNT_LOGITS_BYTES, etc.); §12 non-goals (no kernel driver, no PCIe endpoint, no MMIO simulator, no ASIC finalization, no MSI-X config, no DMA engine, no physical layer); §13 future milestone split M105–M110; Appendix A: BAR0 register map summary table (all 16 offset ranges); Appendix B: 10 open engineering questions (multi-function devices, MSI-X vectors, CQ sharing, fence ID namespace, 64-bit atomicity, counter overflow, trace full policy, Q4 group size, tile memory granularity, routing table ownership); `docs/aimu_pcie_command_requirements.md` intro updated with M104 cross-reference; `docs/aimu_architecture.md` §11 added with BAR0 layout table and M103↔M104 register mapping table; no C, Python, Makefile, binary format, or inference behavior changed.
- Milestone 110: minimal PCIe/AIMU prototype design review — documentation only; `docs/aimu_pcie_prototype_review.md` added: 13-section design review reconciling M103–M109 artifacts; §1 executive summary (proven vs. unproven); §2 prototype objective and 10-point proof criteria; §3 proposed 7-layer prototype stack (layers 1–5 software, layers 6–7 deferred); §4 four MVP options A–D (software sim/MMIO-mock/FPGA/custom board) with cost/risk/what-it-proves; §5 required host-visible capabilities table (M104 register → M105–M108 simulator module); §6 required AIMU tile capabilities (memory model, dtype/op flags, completion behaviour, counter/trace reporting); §7 data movement assumptions (cold-path-only PCIe, hot-path tile-local, fabric traffic model, tensor residency); §8 register/command model review (M103 packet model, M104 register map, M105 CQ, M107 DMA, M108 trace, 6-row gap table); §9 placement pipeline review (tool chain diagram, tile economics table, q4/q8/f32 storage table, capacity feasibility loop); §10 minimal success criteria (10-row pass/fail table); §11 risk table (7 risks with severity and mitigation); §12 recommended next milestones M111–M120; §13 non-goals; Appendix A M104–M110 artifact summary; Appendix B 10 open engineering questions (BAR layout, MSI-X vectors, DMA scatter-gather, fence namespace, 64-bit atomicity, counter overflow, trace full policy, q4 group size, tile memory granularity, routing table ownership); `docs/aimu_pcie_command_requirements.md` M110 row marked complete; no C, Python, Makefile, binary format, or inference behavior changed; 175 PASS 0 FAIL total.
- Milestone 109: placement-report-to-command-plan mapper — `compiler/map_placement_to_commands.py` added; reads M98/M100 placement report JSON and emits a deterministic simulated AIMU command plan for the M105 command queue / M103 command packet model; command plan: LOAD_TENSOR_TILE + VALIDATE_TENSOR per placed tensor (sorted by tile_id, tensor_id, slice_start), TILE_BARRIER per tile (fenced on last VALIDATE), QUERY_COUNTERS + TRACE_SNAPSHOT after all tiles (fenced on last TILE_BARRIER); `--model-id`, `--session-id`, `--plan-json`, `--strict` CLI flags; validation: exits 2 for malformed JSON or missing top-level fields, exits 1 for out-of-range tile_id, missing tensor_name/tensor_id, unsupported dtype, q4 missing/invalid group_size, placement_status != placed in strict mode, capacity_status=FAIL in strict mode; non-strict mode emits WARNING for capacity FAIL tiles; command IDs deterministic and sequential from 1; fence_id chain: LOAD=0, VALIDATE depends on LOAD, BARRIER depends on last VALIDATE, QUERY/TRACE depend on last BARRIER; plan JSON: command_plan_version, header, commands, summary, warnings; summary: commands_by_type/tile, total_tensor_bytes, f32/q8/q4 tensor counts, capacity_failures_observed, warnings_observed; four new fixtures: placement_report_q4_tiny.json, placement_report_capacity_fail.json, placement_report_missing_tensor_id.json (plus existing placement_report_valid.json); `check_placement_cmd_plan_smoke()` added to `tests/test_bench_smoke.c` (9 scenarios: valid exit 0, command count 14, deterministic, q4 group_size/packed/scale in notes, malformed exit non-zero, strict capacity-fail exit 1, non-strict WARNING output, missing tensor_id exit 1, JSON plan keys); `docs/tensor_placement_report.md` §13 added; `docs/aimu_pcie_command_requirements.md` M109 row marked complete; no C, Makefile, binary format, or inference behaviour changed; 175 PASS 0 FAIL total.
- Milestone 108: AIMU command trace and counter integration — `include/att1_aimu_trace.h`, `src/aimu_trace.c`, `tests/test_aimu_trace.c` added; pure C11 in-process unified snapshot layer that aggregates control-plane counters from M105 command queue, M106 device/tile simulator, and M107 DMA descriptor simulator into a single `att1_aimu_trace_snapshot`; sub-structs: `att1_aimu_trace_cmdq_counters` (commands_submitted/completed/failed, queue_full_count, unsupported_commands, fence_value), `att1_aimu_trace_device_counters` (device_resets, tile_resets=sum of per-tile reset_count, tile_errors=count of tiles in TILE_ERROR state), `att1_aimu_trace_dma_counters` (dma_submitted/completed/failed, bytes_h2d/d2h/d2d, alignment_failures, range_failures, unsupported_flags), `att1_aimu_trace_fabric_counters` (packets_sent/received, payload_bytes_sent/received, congestion_events — all zero placeholder for M109+), `att1_aimu_trace_meta` (trace_version=0x00010000, snapshot_id, device_id, tile_count, event_count, dropped_events, status); `ATT1_AIMU_TRACE_MAGIC=0xA771FACE`; status codes: OK=0 (all sources present), PARTIAL=1 (some NULL), EMPTY=2 (no snapshot yet); `att1_aimu_trace_create/destroy`; `att1_aimu_trace_snapshot_cmdq/device/dma` (individual, no snapshot_id increment, do not mutate source counters); `att1_aimu_trace_snapshot_all` (combined, increments snapshot_id, sets status); `att1_aimu_trace_get_snapshot` (copies snapshot); `att1_aimu_trace_reset` (clears trace-local state only, does not modify simulators); `att1_aimu_trace_render` (human-readable text to FILE*: meta header + cmdq/device/dma/fabric sections); `att1_aimu_trace_status_name` helper; register mapping: meta→M104 §8 TRACE_* + §2 DEVICE_ID/TILE_COUNT, cmdq→§4 CQ_* + §7 CNT_COMMANDS_*, device→§2 GLOBAL_STATUS + §3 TILE_STATUS/TILE_RESET_CONTROL, dma→§5 DMA_STATUS + §7 DMA counters, fabric→§6 FABRIC_* (placeholder); docs/aimu_register_map.md §13 M108 row marked complete, §16 added; 34 test cases; 166 PASS 0 FAIL total; no CUDA, binary format, or inference behavior changed.
- Milestone 107: AIMU DMA descriptor simulator — `include/att1_aimu_dma.h`, `src/aimu_dma.c`, `tests/test_aimu_dma.c` added; pure C11 in-process simulator for the DMA buffer descriptor model defined in M104 §5; `att1_aimu_dma_desc` struct exactly 64 bytes (4×u64 host/device/src_device/dst_device addrs, 5×u32 byte_length/descriptor_id/command_id/tensor_id/checksum, 3×u16 dim0/dim1/flags, 3×u8 dtype/quant_group_size/direction + _pad[3]); `att1_aimu_dma_create/destroy`; `att1_aimu_dma_register_host_region` / `att1_aimu_dma_register_device_region` (up to 8 regions each; permissive when none registered); `att1_aimu_dma_validate` (no counter update) and `att1_aimu_dma_submit` (validates + increments counters); validation rules: direction ∈ {0,1,2}, byte_length ∈ [1, 2²⁸], no unknown flag bits (valid mask 0x000F), dtype ∈ {F32=0, Q8=1, Q4=2}, Q4: quant_group_size ∈ {32,64} and byte_length % (group_size/2) == 0, addresses 64-byte aligned, no uint64 overflow, ranges within registered regions, D2D src/dst do not overlap; counters: dma_submitted, dma_completed, dma_failed, bytes_host_to_device, bytes_device_to_host, bytes_device_to_device, alignment_failures, range_failures, unsupported_flags; `att1_aimu_dma_get_counters` / `att1_aimu_dma_reset_counters`; `ATT1_AIMU_DMA_MAGIC=0xA771D4A7`, `ATT1_AIMU_DMA_ALIGN_BYTES=64`, `ATT1_AIMU_DMA_MAX_TRANSFER_BYTES=2²⁸`; docs/aimu_register_map.md §15 added: descriptor-to-register mapping table, flag bits table, validation rules table, dtype alignment table, LOAD_TENSOR_TILE integration example, M106 device capability integration note; §13 future-milestone table updated to mark M107 complete; 31 test cases; 132 PASS 0 FAIL total; no CUDA, binary format, or inference behavior changed.
- Milestone 106: AIMU device discovery simulator — `include/att1_aimu_device.h`, `src/aimu_device.c`, `tests/test_aimu_device.c` added; pure C11 in-process simulator for the device probe and tile enumeration registers defined in M104 (§2 global registers, §3 per-tile windows); `att1_aimu_device_create/destroy` (configurable 1–16 tiles, per-tile memory/KV capacity, dtype/op/feature flags, version); `att1_aimu_device_query_info` returns register_map_version, version, feature_flags, global_status; `att1_aimu_device_query_tile` returns memory_capacity_bytes, kv_capacity_bytes, supported_dtypes, supported_ops, fabric_link_mask, state, reset_count, error_code; `att1_aimu_device_supports_dtype/op` checks all tiles; `att1_aimu_device_tile_supports_dtype/op` checks a single tile; `att1_aimu_device_reset/reset_tile` with per-tile and device-level reset counters; `att1_aimu_device_attach_cmdq/snapshot_counters` integrates M105 command queue counters; name helpers for dtype/op/feature/tile-state values; `ATT1_AIMU_DEVICE_MAX_TILES=16`, `ATT1_AIMU_DEVICE_MAGIC=0xA771DE7C`, `ATT1_AIMU_REGISTER_MAP_VERSION=0x00010000`; default features: CMD_RING|COMP_RING|TRACE|COUNTERS|MULTI_SESSION|FENCE|KV_MMU|PLACEMENT_AWARE; default dtypes: F32|Q8|Q4; default ops: all nine; docs/aimu_register_map.md §14 added: M106 simulator section with struct-to-register mapping table, full API reference, and placement-validation usage examples; M13 future-milestone table updated to mark M106 complete; 20 test cases; 101 PASS 0 FAIL total; no CUDA, binary format, or inference behavior changed.
- Milestone 105: PCIe command queue simulator — `include/att1_aimu_cmdq.h`, `src/aimu_cmdq.c`, `tests/test_aimu_cmdq.c` added; in-process ring-buffer simulator for the M103 64-byte command packet protocol; 16 command types (NOP, LOAD_TENSOR_TILE, VALIDATE_TENSOR, EXEC_MATMUL, EXEC_RMSNORM, EXEC_ROPE, EXEC_ATTENTION, EXEC_FFN, KV_APPEND, KV_READ, FABRIC_SEND, FABRIC_REDUCE, TRACE_SNAPSHOT, TILE_BARRIER, RESET_TILE, QUERY_COUNTERS); 22 result codes; `att1_aimu_cmd` struct exactly 64 bytes (fields reordered to avoid implicit padding: u64s first, then u32s, u16s, u8s); CRC32 checksum computed by `att1_aimu_cmdq_submit`; command/completion ring buffers (power-of-two depth, configurable 1–16 tiles, 1–4096 slots); `att1_aimu_cmdq_counters` (commands_submitted/completed/failed, queue_full/compq_full, resets, unsupported_commands, fence_value); NOP/RESET_TILE/QUERY_COUNTERS/TRACE_SNAPSHOT/TILE_BARRIER dispatch to OK; all EXEC_*/KV_*/FABRIC_*/LOAD_*/VALIDATE_* dispatch to ERR_UNSUPPORTED_OP but still produce a completion; RESET_TILE increments counters.resets; completion ring full causes dispatch to return ATT1_ERR_QUEUE_FULL without silently dropping; 22 test cases pass; Makefile extended with aimu_cmdq source and test; no CUDA, binary format, or inference behavior changed.
- Milestone 103 (full entry): `docs/aimu_pcie_command_requirements.md` added (12 sections, Appendix A): §1 prototype goal (7 proof criteria, ATT-1/CUDA/silicon relationships); §2 host/AIMU control plane; §3 64-byte command packet; §4 15 required command types; §5 data movement model; §6 memory model; §7 execution model; §8 64-byte counter/trace snapshot; §9 placement report relationship; §10 prototype options A–D; §11 non-goals; §12 M104–M110 milestone split; `docs/aimu_architecture.md` §10 added; no C, Python, Makefile, binary format, or inference behavior changed. `docs/aimu_pcie_command_requirements.md` added (12 sections, Appendix A): §1 prototype goal (7 proof criteria, relationships to ATT-1 simulator/CUDA validation/future silicon), §2 host/AIMU control plane (device discovery 32-byte device_info block, tile enumeration 64-byte tile_descriptor array, memory capacity discovery, dtype/op/feature flags, firmware version fields, error and status register), §3 64-byte command packet format (14 named fields: command_id/command_type/tile_id/session_id/dtype/model_id/tensor_id/input_buf/output_buf/kv_position/op_params/fence_ids/trace_flags/checksum/status; address-space encoding in high-4-bits), §4 fifteen required command types (LOAD_TENSOR_TILE 0x01, VALIDATE_TENSOR_TILE 0x02, EXEC_MATMUL 0x10, EXEC_RMSNORM 0x11, EXEC_ROPE 0x12, EXEC_ATTENTION 0x13, EXEC_FFN 0x14, KV_APPEND 0x20, KV_READ 0x21, FABRIC_SEND 0x30, FABRIC_REDUCE 0x31, TRACE_SNAPSHOT 0x40, TILE_BARRIER 0x41, RESET_TILE 0x50, QUERY_COUNTERS 0x51), §5 data movement model (weight DMA once at setup, logit-only PCIe hot path, AIMU-to-AIMU fabric for activations, near-memory execution principle), §6 memory model (tensor/KV/staging/command-queue/trace regions with alignment table; CRC32C validation), §7 execution model (command lifecycle, fences/barriers, dependency ordering, completion records, deterministic trace, error propagation, no-silent-fallback rule), §8 64-byte counter/trace snapshot format mirroring att1_trace_t fields (packets_sent/received, payload_bytes, local_op_count, tensor_bytes_read/written, kv_appends/reads, latency, stall counters, per-token trace hook), §9 relationship to M98–M102 placement reports (capacity check formula, tensor ownership routing, slice→buf_descriptor mapping, routing_requirement→command mapping, reduction_behavior→FABRIC_REDUCE mapping, bandwidth estimation, 16/32/64/128 GiB SKU table), §10 four prototype options A–D (software simulator/FPGA/PCIe ASIC card/future ASIC with cost and timeline), §11 non-goals (no ASIC design, no PCIe driver, no MMIO implementation, no DMA descriptor, no physical layer, no format changes), §12 future milestone split M104–M110 (BAR map sketch, command queue simulator, device discovery simulator, DMA descriptor simulator, trace integration, placement-to-command-plan mapper, design review); `docs/aimu_architecture.md` §10 added: M103 summary, 15-command-type table, PCIe hot-path rule, ATT-1 simulator→command mapping table; no C, Python, Makefile, binary format, or inference behavior changed.

- Milestone 111: AIMU MMIO/register-file simulator — `include/att1_aimu_mmio.h`, `src/aimu_mmio.c`, `tests/test_aimu_mmio.c` added; pure C11 in-process simulator for the full 64 KiB BAR0 register space defined in M104; flat `uint32_t regs[16384]` backing store; `mmio_access_type()` function-based dispatch returns `ACCESS_RO/RW/RW1C/WO/RESERVED` for every BAR0 offset; `write32` enforces: RO→`ATT1_ERR_UNSUPPORTED`, RW→store, RW1C→`regs[off/4] &= ~value`, WO→side-effect dispatch via `mmio_wo_write()`, RESERVED→silently discard; reserved offsets read `0xDEADBEEF` (`ATT1_AIMU_MMIO_RESERVED_RD`); WO side-effects: `GLOBAL_CONTROL` toggles `GLOBAL_STATUS.DEVICE_READY`/`TRACE_ACTIVE`; `RESET_CONTROL.SOFT_RESET_ALL` calls `att1_aimu_device_reset`, re-runs `mmio_set_defaults`+`sync_from_{device,cmdq,trace}`; `RESET_CONTROL.RESET_COUNTERS` `memset`s counter cells (0xB8 bytes at 0x4000) + `att1_aimu_trace_reset`; `RESET_CONTROL.RESET_TRACE` zeroes `TRACE_WRITE_PTR`/`TRACE_DROPPED_EVENTS`; `CQ_DOORBELL` increments `m->doorbell_write_count` and updates `CQ_TAIL` cell; `TILE_RESET_CONTROL[N]` calls `att1_aimu_device_reset_tile(dev, N)` then re-syncs device; `COUNTER_SNAPSHOT_CONTROL` is RW with `SNAP_NOW` (bit 0) self-clearing: triggers `att1_aimu_trace_snapshot_all`/`sync_from_cmdq`, increments `m->snapshot_trigger_count`, stores `value & ~SNAP_NOW`; `att1_aimu_mmio_sync()` calls `mmio_set_defaults`+`sync_from_{device,cmdq,trace}` in order; `sync_from_trace` overrides cmdq-sourced counter cells when snapshot is non-empty; `read64/write64` validate 8-byte alignment then delegate to `read32/write32` pairs; attach API: `attach_device/cmdq/trace`; `render()` prints named registers to `FILE*`; `err_name()` returns status string; `ATT1_AIMU_MMIO_MAGIC=0xA771BB10`, `ATT1_AIMU_MMIO_BAR0_SIZE=0x10000`, `ATT1_MMIO_DEVICE_ID_DEFAULT=(0xA771<<16)|0x0001`; `docs/aimu_register_map.md` §17 added; Makefile extended with `aimu_mmio` to `TEST_NAMES` and `COMMON_SRCS`; 22 test functions, 40+ PASS checks; no CUDA, binary format, or inference behavior changed.
- Milestone 112: AIMU control-plane integration harness — `include/att1_aimu_host.h`, `src/aimu_host.c`, `tests/test_aimu_host.c` added; C11 in-process harness that wires M105 (cmdq) + M106 (device) + M107 (DMA) + M108 (trace) + M111 (MMIO) into one deterministic host-to-AIMU control-plane flow; `att1_aimu_host` struct owns all sub-simulators (device/cmdq/dma/trace/mmio), created/destroyed together; `att1_aimu_host_config` lets callers set tile_count/tile_memory_bytes/tile_kv_bytes/dtypes/ops/cmd_ring_depth; probe flow: `att1_aimu_host_probe_device` reads `DEVICE_ID`, `REGISTER_MAP_VERSION`, `TILE_COUNT`, `GLOBAL_STATUS` via MMIO; returns `ATT1_ERR_STATE` if `DEVICE_READY` bit not set; `att1_aimu_host_enumerate_tiles` calls `att1_aimu_device_query_tile` per tile and fills `att1_aimu_host_tile_info[]`; `att1_aimu_host_setup_cmdq` attaches cmdq to device + MMIO + re-syncs; guarded by `h->probed`; `att1_aimu_host_validate_dma` delegates to `att1_aimu_dma_validate`; `att1_aimu_host_submit_cmd` guards on `h->cmdq_ready` then calls `att1_aimu_cmdq_submit`; `att1_aimu_host_ring_doorbell` writes `ATT1_MMIO_CQ_DOORBELL` via MMIO using current `cmd_tail`; `att1_aimu_host_process_one` calls `att1_aimu_cmdq_dispatch_one`; `att1_aimu_host_drain` calls `att1_aimu_cmdq_dispatch_all`; `att1_aimu_host_read_completion` calls `att1_aimu_cmdq_poll_completion`; `att1_aimu_host_snapshot_counters` calls `att1_aimu_trace_snapshot_all` then `att1_aimu_mmio_sync`; `att1_aimu_host_get_summary` aggregates counters from cmdq/dma/mmio/trace into flat `att1_aimu_host_summary` struct; `att1_aimu_host_reset` calls `device_reset`, `mmio_reset`, `trace_reset`, `dma_reset_counters`, `mmio_sync` and clears `probed`/`cmdq_ready`; `att1_aimu_host_render` delegates to `att1_aimu_mmio_render`; `ATT1_AIMU_HOST_MAGIC=0xA771A057`; 20 test functions covering lifecycle, probe identity, probe-not-ready guard, tile enumeration, setup guards, tensor-load flow, VALIDATE_TENSOR, QUERY_COUNTERS, trace snapshot, invalid tile_id, bad DMA descriptor, EXEC_MATMUL→UNSUPPORTED_OP, doorbell guard, submit guard, FIFO order, drain, reset, summary, NULL args, no CUDA dep; Makefile extended with `aimu_host` in `TEST_NAMES` and `$(SRC_DIR)/aimu_host.c` in `COMMON_SRCS`; no CUDA, binary format, or inference behavior changed.
- Milestone 113: AIMU placement-command replay tool — `tools/att1-aimu-replay.c` (C tool, ~600 lines) added: reads a M109 command-plan JSON with a line-by-line state-machine parser (no external JSON library), creates the M112 `att1_aimu_host` harness, drives the full probe→enumerate→setup→DMA-validate→submit→doorbell→dispatch→completion loop for each command in the plan, emits a text report and optional `--report-json` JSON report; accepted command types: all 16 M105 types; result normalisation: `ATT1_AIMU_ERR_UNSUPPORTED_OP` completion accepted when plan expects `ATT1_AIMU_ERR_OK` (matches documented M112 mock behaviour for LOAD_TENSOR_TILE/VALIDATE_TENSOR/EXEC_* stubs); `--strict` rejects plans whose header status != "pass" and stops on first UNSUPPORTED_OP; exit codes: 0=pass, 1=replay failures, 2=parse error; report fields: plan_path, model_id, session_id, tile_count, command_count, commands_replayed, completions_seen, failed_commands, unsupported_commands, dma_validations, doorbell_count, fence_final, trace_event_count, commands_by_type[16], commands_by_tile{}, status, notes; `compiler/replay_aimu_command_plan.py` Python wrapper: validates plan JSON structure before invoking binary (checks command_plan_version, required top-level/header/command keys, tile_id range, command_id monotonicity, dtype, Q4 group_size, fence_id non-negative); exits 2 on validation failure; delegates to `build/att1-aimu-replay`; CLI: `--plan PATH [--tiles N] [--strict] [--report-json PATH]`; `compiler/fixtures/plan_exec_matmul_unsupported.json` fixture added (1-command EXEC_MATMUL plan with expected_status=ATT1_AIMU_ERR_UNSUPPORTED_OP); Makefile extended: `AIMU_REPLAY_BIN`, `AIMU_REPLAY_OBJS` variables, build rule, added to `all`, `test`, `test-verbose` prerequisites; `check_aimu_replay_smoke()` added to `tests/test_bench_smoke.c` (12 subtests: valid plan exits 0, command_count=14, deterministic, replay counters in text, JSON report has all required keys, non-strict unsupported-op exits 0, strict unsupported-op exits 1, malformed JSON exits non-zero, Python wrapper delegates to binary, Python wrapper malformed JSON, Python wrapper missing field, no CUDA dependency); all existing tests remain green; tagged milestone-113-aimu-replay-tool.
- Milestone 114: AIMU fabric routing requirements — documentation only; `docs/aimu_fabric_routing.md` added (~330 lines, 10 sections): §1 fabric role (what moves: activations, partial logits, reduction payloads, KV payloads, trace/control; what stays local: weight tiles, KV/session memory, scratchpad); §2 routing model (route descriptor fields: route_id, source_tile, dest_tile_mask, packet_type, payload_type, payload_bytes, fence_id, completion_fence_id, reduction_id, trace_id; ordering and barrier requirements); §3 eight packet/route types with Phase 1/2/3 capability table (ACTIVATION_SEND, ACTIVATION_BROADCAST, PARTIAL_REDUCE, LOGITS_REDUCE, KV_TRANSFER, TILE_BARRIER, TRACE_EVENT, CONTROL_ACK); §4 placement-driven routing topologies (layer-wise pipeline, row-split matmul, column-split matmul, head-wise attention split, lm_head/vocab split, embedding lookup routing, replicated norms, KV ownership and routing policy); §5 reduction semantics (sum tile-ID-ordered for determinism, concat, future max/top-K reserved, pass-through, tolerance table: f32 exact / q8 abs≤0.15 / q4 abs≤0.35); §6 fabric counters (per-tile packet/byte, per-packet-type[8], reduction started/completed/partial-arrivals, barrier started/completed/arrivals, congestion/stall, route failures/dropped/ordering violations; M108 snapshot integration note); §7 twelve validation rules F1–F12 (tile range, non-empty dest, nonzero payload, known type, reduction explicit, no missing route, acyclic DAG, barrier ordering, fence ordering, bandwidth compatibility, concat completeness, reduction participant completeness); §8 relationship to existing simulator (Phase 1 att1_fabric_t API→route type mapping table, cluster inference counter mapping, M109 command plan integration, M115–M120 implementation path table); §9 non-goals (no hardware fabric, no PCIe driver change, no runtime change, no new kernels, no CUDA, no format change, no C/Python/Makefile changes); §10 future milestone split M115–M120; `docs/aimu_architecture.md` §12 added: M114 summary, §12.1 spec overview, §12.2 relationship to §9/§10/§11/fabric.md/aimu_pcie_command_requirements.md; `docs/aimu_pcie_command_requirements.md` §12 milestone split table: M114 row added (complete, full summary); make test remains green; tagged milestone-114-aimu-fabric-routing.
- Milestone 115: AIMU fabric route schema and report format — documentation/spec/fixture only; `docs/aimu_fabric_routing.md` §11 added (~380 lines): §11.1 report header (key-value and JSON formats; fields: route_report_version, source_placement_report, source_command_plan, model_name, model_id, session_id, tile_count, route_count, packet_count_estimate, payload_bytes_estimate, fabric_policy, status, report_timestamp); §11.2 route records (JSON format, 17-field reference table: route_id, route_type, source_tile, destination_tiles, source_tensor, source_command_id, destination_tensor, destination_command_id, payload_type, payload_bytes, packet_count_estimate, dependency_fence, reduction_id, reduction_behavior, ordering_policy, trace_id, route_status; reduction_behavior and ordering_policy value tables); §11.3 per-tile route summary (JSON format, 11-field reference table: outbound/inbound packet counts and payload bytes, reductions_started/completed, barriers_started/completed, route_failures, estimated_bandwidth_gib_sec); §11.4 warnings and failures (diagnostic codes table: INVALID_TILE_TARGET/EMPTY_DESTINATION/ZERO_PAYLOAD/UNKNOWN_PACKET_TYPE/REDUCTION_NOT_EXPLICIT/MISSING_ROUTE/CYCLIC_DEPENDENCY/BARRIER_ORDERING/FENCE_ORDERING/BANDWIDTH_OVERFLOW/CONCAT_INCOMPLETE/REDUCTION_PARTICIPANT_MISMATCH/UNKNOWN_COMMAND_REF/MISSING_REDUCTION_BEHAVIOR/UNSUPPORTED_PACKET_TYPE; severity/rule mapping); §11.5 human-readable report format (five tables: route summary, per-tile traffic, reduction summary, barriers/fences, warnings/failures; exact column layout defined); §11.6 JSON report format (five top-level keys: header/routes/tiles/warnings/failures; stability rules: no rename without version bump, optional fields null, unknown keys ignored, version checked first); §11.7 relationship to future tools (M116 validator, M117 command-plan mapper, M118 bandwidth simulator, M119 replay integration, M120 go/no-go); §11.8 fixture description (fabric_route_report_tiny.json: 3 routes 2 tiles 512 bytes 0 failures); §11.9 map_placement_to_fabric_routes.py tool spec (CLI, algorithm, exit codes, strict mode); `docs/tensor_placement_report.md` §14 added (~80 lines): §14.1 placement-record-to-route mapping table (routing_requirement/reduction_behavior field→route field); §14.2 payload byte estimation formulas per route type; §14.3 packet count estimate formula; §14.4 fabric bandwidth estimate formula (F10 cross-check); §14.5 fixture cross-reference; §14.6 tool spec summary; §14.7 non-goals; `compiler/fixtures/fabric_route_report_tiny.json` added: 3-route 2-tile f32 layer-wise reference fixture (route_id 1 ACTIVATION_SEND tile_0→1 256B, route_id 2 ACTIVATION_SEND tile_1→0 256B, route_id 3 TILE_BARRIER 0B; per-tile summaries; 0 warnings 0 failures status=pass); `docs/OPERATION_LOG.md` updated; no C, Python, Makefile, binary format, or inference behavior changed; make test: 342 PASS 0 FAIL; tagged milestone-115-aimu-fabric-route-schema.
- Milestone 116: AIMU fabric route validator — `compiler/validate_fabric_routes.py` added (~430 lines): pure Python 3 validator for M115 fabric route report JSON; four validation phases: (1) header — route_report_version supported, tile_count positive, route_count/packet_count_estimate/payload_bytes_estimate non-negative, status recognized, --strict rejects non-pass status; (2) route records — route_id present and unique (DUPLICATE_ROUTE_ID), route_type recognized for 8 types (UNSUPPORTED_PACKET_TYPE), source_tile in [0,tile_count) (INVALID_TILE_TARGET), destination_tiles non-empty for data/barrier types (EMPTY_DESTINATION), no self-routing for send/reduce types (SELF_ROUTE), payload_bytes > 0 for data routes (ZERO_PAYLOAD), packet_count_estimate > 0 for data routes (ZERO_PACKET_COUNT), reduction_behavior recognized (UNKNOWN_REDUCTION_BEHAVIOR), PARTIAL_REDUCE/LOGITS_REDUCE require explicit reduction_behavior (REDUCTION_NOT_EXPLICIT), reduction_id present for reduction routes (MISSING_REDUCTION_ID), ordering_policy recognized (UNKNOWN_ORDERING_POLICY), TILE_BARRIER must use barriered ordering (BARRIER_ORDERING), route_status recognized; (3) per-tile summaries — tile_id unique and in range (INVALID_TILE_ID/DUPLICATE_TILE_ID), all 9 counter fields non-negative integers (MISSING_TILE_FIELD/INVALID_TILE_FIELD), estimated_bandwidth_gib_sec finite non-negative when present (INVALID_BANDWIDTH); (4) consistency — header.route_count vs actual list (ROUTE_COUNT_MISMATCH error), per-tile outbound/inbound packet counts vs route sums (TILE_OUTBOUND_MISMATCH/TILE_INBOUND_MISMATCH warnings), warnings/failures lists are arrays (INVALID_DIAG_LIST); 37 diagnostic codes total; CLI: --report PATH [--report-json PATH] [--strict]; human-readable output: path/tile_count/route_count/warning_count/error_count/status; optional JSON summary (status/total_warnings/total_errors/warnings/failures/checked); exit 0=pass, 1=violations, 2=parse error; `compiler/fixtures/fabric_route_invalid_tile.json` added (dest tile 99 out of range); `compiler/fixtures/fabric_route_duplicate_id.json` added (two routes with route_id=1); `compiler/fixtures/fabric_route_zero_payload.json` added (ACTIVATION_SEND payload_bytes=0); `compiler/fixtures/fabric_route_missing_reduction.json` added (PARTIAL_REDUCE with reduction_behavior="none"); `compiler/fixtures/fabric_route_count_mismatch.json` added (header.route_count=5, list length=1); `docs/aimu_fabric_routing.md` §12 added: §12.1 validator overview/CLI; §12.2 four validation phases; §12.3 F1–F12 rule coverage table (F7/F9/F10/F11 deferred to M117–M120); §12.4 37-code diagnostic code table; §12.5 6-fixture table with expected results; §12.6 non-goals; §10 M116 row marked complete; `docs/OPERATION_LOG.md` updated; no C, Makefile, binary format, or inference behavior changed; make test: 342 PASS 0 FAIL; tagged milestone-116-aimu-fabric-route-validator.
- Milestone 117: command-plan-to-fabric-route mapper — `compiler/map_commands_to_fabric_routes.py` added (~430 lines): pure Python 3; reads M109 AIMU command plan JSON; emits M115-compatible fabric route report JSON; planner/report generator only — does NOT execute routes or change inference behavior; four validation phases before mapping: command_plan_version check, header tile_count/command_count, command_id uniqueness, tile_id range, unknown command types warned; route generation rules by command type: LOAD_TENSOR_TILE/EXEC_*/KV_*/NOP/RESET_TILE → no route (local only); VALIDATE_TENSOR → CONTROL_ACK (64B control payload, source tile→all other tiles, ordered); TILE_BARRIER → TILE_BARRIER (0B barrier token, source→all tiles incl. self, barriered); QUERY_COUNTERS → CONTROL_ACK (64B counter snapshot, ordered); TRACE_SNAPSHOT → TRACE_EVENT (64B trace payload, ordered); FABRIC_SEND → ACTIVATION_SEND (payload from total_bytes or placement report or 1024B default); FABRIC_REDUCE → PARTIAL_REDUCE (payload from total_bytes/placement/default; reduction_behavior inferred from notes or defaults to sum; deterministic reduction_id counter); payload resolution priority: command total_bytes → tensor_id→placement lookup → tensor_name→placement lookup → 1024B default (warning); per-tile summaries derived from emitted routes (outbound/inbound packet counts and bytes from route records; barriers/reductions from route type); generated reports always pass M116 per-tile consistency checks; report header: route_report_version=1, source_command_plan, source_placement_report, fabric_policy=layer_wise, status=pass/warn/fail; CLI: --plan PATH [--placement-report PATH] [--report-json PATH] [--tokens-per-sec N] [--fabric-gib-sec N] [--strict]; exit 0=success, 1=structural error/strict violation, 2=parse error; round-trip test: plan→mapper→validator exits 0; `compiler/fixtures/plan_tiny_barrier_trace.json` added: 3-command minimal plan (TILE_BARRIER+TRACE_SNAPSHOT+QUERY_COUNTERS, 2 tiles, command_count=3); `compiler/fixtures/fabric_route_from_plan_tiny.json` added: 3-route report generated from above (TILE_BARRIER+TRACE_EVENT+CONTROL_ACK, 2 tiles, payload_bytes_estimate=128, passes M116); `docs/aimu_fabric_routing.md` §13 added: §13.1 mapper overview/CLI; §13.2 route generation rules table; §13.3 per-tile summary generation; §13.4 report header fields; §13.5 fixtures; §13.6 M116 relationship/round-trip; §13.7 non-goals; §10 M117 row marked complete; `docs/OPERATION_LOG.md` updated; no C, Makefile, binary format, or inference behavior changed; make test: 342 PASS 0 FAIL; tagged milestone-117-command-plan-to-fabric-route-mapper.
- Milestone 118: fabric bandwidth and latency simulator — `compiler/simulate_fabric_bandwidth.py` added (~370 lines): pure Python 3; reads M115/M117 fabric route report JSON; emits per-route, per-tile, and aggregate bandwidth/latency pressure estimates; planner-level report tool only — does NOT execute routes or change inference behavior; all estimates are deterministic model projections, not measured hardware behavior; per-route model: effective_payload_bytes from route record; packet_count from packet_count_estimate (0 for TILE_BARRIER); packet_overhead_bytes = flag × packet_count; total_wire_bytes = payload + overhead; estimated_bandwidth_gib_sec = total_wire × tokens_per_sec / GiB; estimated_latency_ns = base_latency_ns + hops × per_hop_latency_ns; hops=0 for TILE_BARRIER/CONTROL_ACK (local/control routes); hops=1 for all other route types (cross-tile); per-tile model: outbound/inbound wire bytes and bandwidth accumulated from routes; bottleneck_status from max(out_bw, in_bw); aggregate model: total_payload_bytes_per_token, total_wire_bytes_per_token, required_fabric_gib_sec = total_wire × tokens_per_sec / GiB, utilization_percent, bottleneck_route_id, bottleneck_tile_id; status rules: PASS ≤80% utilization, WARN ≤100%, FAIL >100%, UNKNOWN if no --fabric-gib-sec; --strict exits nonzero on WARN or FAIL; without strict, only FAIL exits nonzero (exit 1); PASS/UNKNOWN exit 0; rule-based recommendations: increase bandwidth (WARN/FAIL), reduce token rate (FAIL), reduce cross-tile placement if activation routes dominate, prefer head-wise attention placement, split lm_head/logits if logits routes dominate, reduce KV pressure if KV routes dominate; CLI: --route-report PATH [--target-tokens-per-sec N] [--fabric-gib-sec N] [--base-latency-ns N] [--per-hop-latency-ns N] [--packet-overhead-bytes N] [--report-json PATH] [--strict]; exit 0=PASS/UNKNOWN, 1=FAIL/WARN+strict/structural error, 2=parse error, 3=invalid numeric flag; `compiler/fixtures/bw_sim_tiny.json` added: checked-in simulator JSON for fabric_route_from_plan_tiny.json at 1 token/s and 32 GiB/s (3 routes, status=PASS); `docs/aimu_fabric_routing.md` §14 added: §14.1 overview/CLI/exit codes; §14.2 simulator model tables (per-route/per-tile/aggregate); §14.3 latency model; §14.4 status rules; §14.5 output description; §14.6 recommendations table; §14.7 fixtures and round-trip test; §14.8 non-goals; §10 M118 rows marked complete; `docs/OPERATION_LOG.md` updated; no C, Makefile, binary format, or inference behavior changed; make test: 342 PASS 0 FAIL; tagged milestone-118-aimu-fabric-bw-sim.
- Milestone 119: placement, command, and fabric replay integration — `compiler/run_aimu_planning_pipeline.py` added (~380 lines): pure Python 3; end-to-end prototype planning pipeline; chains M99+M101+M109+M113+M117+M116+M118 tools via subprocess; 8 pipeline stages: (1) validate_tensor_placement_report.py → placement validation JSON; (2) propose_tensor_placement.py → advisory JSON; (3) map_placement_to_commands.py → command plan JSON; (4) replay_aimu_command_plan.py → replay report JSON; (5) map_commands_to_fabric_routes.py → fabric route JSON; (6) validate_fabric_routes.py → route validation JSON; (7) simulate_fabric_bandwidth.py → bandwidth simulation JSON; (8) integrated summary; planning and control-plane simulation only — does NOT execute inference, access real PCIe/MMIO registers, change .att1 binary format, or implement a kernel driver; strict mode halts at stages 1/2/4/6 FAIL and exits 1; non-strict mode continues past advisory FAIL so all stage reports are emitted, final status=fail; stage 3 always halts on structural error (required by downstream stages); intermediate files written to --workdir or temp directory (cleaned on exit); integrated JSON report fields: pipeline_version, placement_report_path, per-stage statuses (placement_validation_status/advisory_status/command_plan_status/command_replay_status/fabric_route_map_status/fabric_route_validation_status/fabric_simulation_status), tile_count, tensor_count, command_count, route_count, commands_replayed, completions_seen, failed_commands, unsupported_commands, aggregate_required_fabric_gib_sec, fabric_utilization_percent, capacity_status, bandwidth_status, recommended_next_action, status; rule-based recommendations: advisory fail→report next_action, advisory warn→suggest remediation, replay fail→review unsupported ops, route val fail→fix routes, bw fail→increase bandwidth or reduce token rate, bw warn→consider budget increase, all pass→ready for prototype evaluation; CLI: --placement-report PATH [--model-id ID] [--session-id ID] [--target-tokens-per-sec N] [--fabric-gib-sec N] [--workdir PATH] [--report-json PATH] [--strict]; exit 0=PASS/WARN, 1=FAIL/WARN+strict/stage error, 2=parse/missing file, 3=invalid flag; tested: placement_report_valid.json pipeline passes (all stages pass, exit 0); placement_report_capacity_fail.json produces overall FAIL in non-strict mode (emits full report, exit 1); --strict stops pipeline at advisory FAIL (exit 1); /dev/null input produces FAIL (exit 1); missing file exits 2; invalid flag exits 3; JSON report contains all required stage statuses; `docs/aimu_pcie_prototype_review.md` §14 added: integrated pipeline overview, 8-stage table, CLI flags/exit codes, stage halt behavior, integrated report fields, recommendation rules, non-goals; `docs/OPERATION_LOG.md` updated; no C, Makefile, binary format, or inference behavior changed; make test: 342 PASS 0 FAIL; tagged milestone-119-aimu-planning-pipeline.
- Milestone 120: Phase 3 AIMU/PCIe prototype go/no-go review — documentation only; `docs/aimu_phase3_go_no_go.md` added (~300 lines): §1 executive summary (CONDITIONAL GO decision; Option B recommended; FPGA deferred; Options D/E not recommended); §2 current proven capabilities table (40 rows, M6–M119, all simulator and planning pipeline artifacts); §3 current unproven areas table (11 gaps: no real PCIe endpoint, no kernel driver, no FPGA, no real DMA, no hardware fabric, no physical tile memory, no power/thermal measurements, no tensor-level placement execution, no hardware-backed inference, no NoC arbitration model, no hardware fences); §4 prototype options A–E with what-it-proves/complexity/risk/cost/recommendation for each: Option A (software sim, already complete, no new work required), Option B (userspace MMIO emulator, RECOMMENDED NEXT, moderate complexity, zero hardware cost), Option C (FPGA, HIGH complexity, USD 5K–30K, defer until Option B stable), Option D (custom board, VERY HIGH complexity, USD 50K–500K+, NOT RECOMMENDED), Option E (ASIC, EXTREMELY HIGH, multi-year/multi-million, NOT RECOMMENDED); §5 recommended path (6 steps: Option B → command-plan replay → fabric route replay → freeze command/control model → FPGA feasibility note → maintain 16/32/64/128 GiB SKU modeling); §6 go/no-go criteria table (12 rows: 8 already met through M119, 4 not yet met targeting M121–M124); §7 hardware economic notes for 16/32/64/128 GiB tile SKUs (KV pressure, fabric pressure, model fit, cost class); AIMU ASIC note: same ASIC should support runtime-discoverable memory capacity via TILE_MEMORY_CAPACITY register pair; §8 risk table (8 risks: memory market volatility, PCIe latency hiding, fabric routing complexity, KV-cache pressure, q4 accuracy/divergence, thermal/power unknowns, placement model optimistic assumptions, patent-sensitive details must remain local); §9 decision: CONDITIONAL GO with specific authorization scope (Option B authorized; FPGA NOT authorized until §6 gate met; Option D/E on hold; Option A complete); §10 next milestone proposals M121–M127 (userspace MMIO emulator workflow, command-plan replay against MMIO emulator, fabric route replay simulator, tile memory allocator simulator, tensor-level placement execution plan, FPGA feasibility research notes, Phase 3 BOM and board options review); §11 non-goals; `docs/aimu_pcie_prototype_review.md` §12 M119 and M120 rows marked COMPLETE; `docs/OPERATION_LOG.md` updated; no C, Python, Makefile, binary format, or inference behavior changed; make test: 342 PASS 0 FAIL; tagged milestone-120-phase3-go-no-go.
- Milestone 121: Userspace MMIO/register-file emulator workflow — `include/att1_aimu_userspace.h`, `src/aimu_userspace.c`, `tools/att1-aimu-mmio-emulator.c`, `tests/test_aimu_userspace.c` added; pure C11; wraps the M112 `att1_aimu_host` harness and exposes the 64 KiB BAR0 register window through a mmap-backed file (Option B from M120 go/no-go); satisfies the M120 CONDITIONAL GO gate for the userspace emulator path; `att1_aimu_userspace_open()` validates config, creates the M112 host, opens the BAR0 file with `O_RDWR|O_CREAT`, `ftruncate`s to `ATT1_AIMU_MMIO_BAR0_SIZE` (64 KiB), `mmap`s `MAP_SHARED`, and flushes initial register state; `att1_aimu_userspace_close()` flushes, `munmap`s, closes fd, destroys host; all register reads/writes route through M111 MMIO simulator semantics (RO enforcement, WO side-effects, RW1C clear-on-write) — not raw mmap mutation; flush_bar0() called after every state-changing operation (`att1_aimu_mmio_sync` + `memcpy` + `msync`); API covers full smoke flow: `open/close/probe/enumerate_tiles/setup_cmdq/read32/write32/validate_dma/submit_cmd/ring_doorbell/drain/snapshot/get_summary/flush_bar0/print_summary`; memory guardrail enforced: `tile_memory_bytes` stored in `TILE_MEMORY_CAPACITY_*` registers only, no huge buffer allocation; max `tile_memory_bytes` = 256 MiB register limit; `tile_count > 16` rejected; DMA descriptors are descriptor-only (64-byte mock, no actual tensor data); CLI tool `att1-aimu-mmio-emulator`: `--bar0-file PATH`, `--tiles N`, `--tile-memory-mib N`, `--kv-memory-mib N`, `--run-smoke`, `--report-json PATH`, `--verbose`; smoke flow: probe → read DEVICE_ID/REGISTER_MAP_VERSION/TILE_COUNT → enumerate → setup_cmdq → NOP+LOAD_TENSOR_TILE+VALIDATE_TENSOR+QUERY_COUNTERS → ring_doorbell → drain → snapshot → summary; 15 test functions, 77 assertions, all PASS; `docs/aimu_pcie_prototype_review.md` §12 M121 row marked COMPLETE, §15 added with design rationale, BAR0 mmap design, memory guardrail, smoke flow, CLI usage, non-goals; Makefile: `aimu_userspace.c` added to `COMMON_SRCS`, `aimu_userspace` to `TEST_NAMES`, `AIMU_EMULATOR_BIN`/`AIMU_EMULATOR_OBJS` added, build rule, `all`/`test`/`test-verbose` updated; no inference, CUDA, .att1 format, or tokenizer behavior changed; make test: 419 PASS 0 FAIL; tagged milestone-121-aimu-userspace-emulator.
- Milestone 122: Command-plan replay against userspace MMIO emulator — `tools/att1-aimu-mmio-replay.c`, `compiler/replay_command_plan_via_mmio.py`, `compiler/fixtures/plan_mmio_smoke.json`, `tests/test_aimu_mmio_replay.c` added; pure C11 tool and Python 3 wrapper; extends M113 replay path so M109 command-plan JSON files drive the M121 `att1_aimu_userspace` interface instead of M112 `att1_aimu_host` directly; this is userspace emulator replay — NOT real PCIe/hardware; M113 in-process replay path (`att1-aimu-replay`) unchanged; `att1-aimu-mmio-replay` CLI: `--plan PATH [--bar0-file PATH] [--tiles N] [--tile-memory-mib N] [--kv-memory-mib N] [--strict] [--report-json PATH] [--verbose]`; per-command flow: map command_type string → enum, validate tile_id vs emulator tile_count, register synthetic DMA regions via `u->host->dma` for LOAD_TENSOR_TILE (descriptor-only, no real tensor data), `att1_aimu_userspace_submit_cmd`, `att1_aimu_userspace_ring_doorbell`, `att1_aimu_host_process_one(u->host)`, `att1_aimu_userspace_flush_bar0`, `att1_aimu_host_read_completion(u->host, &comp)`; after probe: reads DEVICE_ID and REGISTER_MAP_VERSION from BAR0 and verifies register_map_version == ATT1_AIMU_REGISTER_MAP_VERSION; verifies emulator tile_count >= plan tile_count; exit codes: 0=pass, 1=failure/strict violation, 2=parse/arg error; text and optional JSON report emitted (JSON adds bar0_file, mmio_doorbell_count, device_id, register_map_version, mmio_tile_count fields vs M113); Python wrapper `replay_command_plan_via_mmio.py`: same validation logic as M113 wrapper + `tile_count > 16` guard (M121 emulator limit), passes all CLI flags through to C binary; fixture `plan_mmio_smoke.json`: 4-command plan (NOP, QUERY_COUNTERS, TILE_BARRIER, TRACE_SNAPSHOT, tile_count=2, status=ok); 10 test cases in `test_aimu_mmio_replay.c`: smoke (4-command plan via emulator API), completion_count_matches, fence_monotonic, doorbell_increments, counter_snapshot, invalid_tile_id_fails, register_map_version_ok, device_id_matches, mmio_tile_count_register, no_cuda_dep; `docs/aimu_pcie_prototype_review.md`: §12 M122 row added COMPLETE, §16 added with overview, deliverables, CLI usage, key differences from M113, non-goals; Makefile: `AIMU_MMIO_REPLAY_BIN`, `AIMU_MMIO_REPLAY_OBJS` added, build rule, `aimu_mmio_replay` to `TEST_NAMES`, `all`/`test`/`test-verbose` updated; no inference, CUDA, .att1 format, or tokenizer behavior changed; M113 replay path unaffected; tagged milestone-122-aimu-mmio-replay.
- Milestone 123: AIMU fabric route replay simulator — `compiler/replay_fabric_routes.py` added (~340 lines): pure Python 3 deterministic replay simulator for M115/M116/M117 fabric route reports; validates using M116 rules (direct import of `validate_fabric_routes`); replays routes in `route_id` order; accumulates per-tile counters (packets_sent, packets_received, payload_bytes_sent, payload_bytes_received, reductions_started, reductions_completed, barriers_started, barriers_completed, trace_events, route_failures); groups PARTIAL_REDUCE/LOGITS_REDUCE routes by `reduction_id`; marks group complete when any route in group has explicit reduction_behavior (sum/concat/max/topk); TILE_BARRIER: 1 barrier_started + 1 barrier_completed per route on source tile; integrates M118 bandwidth/latency via direct import of `simulate_fabric_bandwidth` functions; text report (stdout) + optional `--report-json`; exit 0=pass, 1=fail/strict, 2=parse error; CLI: `--route-report PATH [--target-tokens-per-sec N] [--fabric-gib-sec N] [--strict] [--report-json PATH]`; `compiler/fixtures/fabric_route_reduction_tiny.json` added (3-route, 2-tile, 2×PARTIAL_REDUCE with reduction_behavior=sum + TILE_BARRIER, passes M116 validator); `check_fabric_route_replay_smoke()` added to `tests/test_bench_smoke.c` (10 subtests: tiny fixture exits 0, deterministic, aggregate counters present, invalid tile exits non-zero, zero payload exits non-zero, missing reduction behavior exits non-zero, reduction fixture reports barriers/reductions with JSON keys, strict mode fails on validation error, malformed JSON exits non-zero, no tracked Python cache artifacts); `docs/aimu_fabric_routing.md` §15 added (6 subsections: role/scope, CLI, per-tile counters table, replay report fields table, fixtures table, non-goals); `docs/aimu_pcie_prototype_review.md` §12 M123 row added COMPLETE, pipeline table updated (stage 7 now replay_fabric_routes.py, stage 8 simulate_fabric_bandwidth.py, stage 9 integrated); no C, Makefile, binary format, or inference behavior changed; baseline 472 PASS 0 FAIL maintained; tagged milestone-123-aimu-fabric-route-replay.
- Milestone 124: AIMU tile memory allocator simulator — `include/att1_aimu_mem.h`, `src/aimu_mem.c`, `tests/test_aimu_mem.c` added; pure C11 simulated per-tile address-space allocator; capacity_bytes is metadata only — no backing buffer of capacity_bytes size is ever allocated (64 GiB metadata test completes instantly without large malloc); simulated address space starts at ATT1_AIMU_MEM_BASE (0x0); first-fit linear scan allocator; 9 region types: TENSOR, KV_CACHE, STAGING, DMA_BUFFER, COMMAND_QUEUE, COMPLETION_QUEUE, TRACE_BUFFER, FABRIC_BUFFER, RESERVED; allocation flags: READ_ONLY, WRITE_ONLY, PINNED, DMA_TARGET (unknown flag bits rejected); alignment range [16 B, 64 KiB], power-of-two required, default=16 B; overflow-safe range arithmetic throughout; `att1_aimu_mem_alloc_range()` returns simulated base_address and alloc_id; `att1_aimu_mem_free()` rejects double free with ATT1_ERR_STATE; `att1_aimu_mem_query_by_id()` and `att1_aimu_mem_query_by_address()` return const record pointer; `att1_aimu_mem_range_valid()` checks if [addr, addr+size) is contained within a single live allocation (for future M125/M126 DMA descriptor range cross-checking); `att1_aimu_mem_get_frag()` reports used_bytes, free_bytes, largest_free_block, allocation_count, fragmentation_pct; `att1_aimu_mem_reset()` clears all allocations; `att1_aimu_mem_render()` prints human-readable table to FILE*; 16 test functions, 78 PASS 0 FAIL; M107 DMA behavior unchanged (integration deferred to M125/M126); `aimu_mem` added to TEST_NAMES and COMMON_SRCS in Makefile; no inference, CUDA, .att1 format, or tokenizer behavior changed; tagged milestone-124-aimu-tile-mem-alloc.
- Milestone 125: Tensor-level placement execution plan — `docs/tensor_execution_plan.md` added (~380 lines): §1 scope (planning/advisory only; no inference, hardware, or backend changes); §2 execution phases table (14 phases: DEVICE_PROBE through CLEANUP/RESET); §3 per-layer command sequence tables for attention sublayer (18 steps incl. RMSNorm, QKV matmul, RoPE, KV append/read, attention, output proj, residual, barrier) and FFN sublayer (7 steps: ffn_norm, gate/up matmul, SwiGLU, down proj, residual, barrier) and LM head (output norm, LM head matmul, softmax); §4 placement-dependent execution (9 policies: layer_wise, tensor_wise, row_split, column_split, head_wise, lm_head_split, embedding_split, replicated_norms, KV_ownership); §5 fabric/reduction requirements table (7 route types with payload source, reduction_behavior, barrier/fence, ordering rule, tolerance implications); §6 M124 region type → execution mapping table (8 region types with size estimate and alignment); §7 execution-plan JSON record schema v1 (top-level + command record with 15 fields); §8 8 validation/failure rules; §9 planning tool CLI (`--placement-report`, `--command-plan`, `--route-report`, `--model-id`, `--session-id`, `--token-phase`, `--plan-json`, `--strict`); §9.2 pipeline position diagram (M98→M109→M115→M125→M128→M129→M130→M131); §10 fixtures; §11 non-goals; `compiler/plan_tensor_execution.py` added (~440 lines): pure Python 3 advisory planner; reads M98/M100 placement report (required), M109 command plan (optional), M115/M117 route report (optional); generates EXECUTION_PLAN_VERSION=1 JSON with per-tile, per-layer EXEC_* command records; load + validate phase generates LOAD_TENSOR_TILE + VALIDATE_TENSOR per tile; prefill setup generates TILE_BARRIER + TRACE_SNAPSHOT on all tiles; per-layer plan generates 18 attention + 7 FFN commands per layer with fence chain; TRACE_SNAPSHOT every 4 layers; LM head plan (output norm + matmul + softmax); QUERY_COUNTERS cleanup; route index groups route_ids by route_type for PARTIAL_REDUCE/LOGITS_REDUCE/ACTIVATION_SEND integration; placement_policy warnings for head_wise with no PARTIAL_REDUCE routes; strict mode promotes WARN to ERROR; exit 0=pass, 1=error/strict+warn, 2=parse error; smoke test with placement_report_valid.json: 57 commands, status=pass; no C, Makefile, binary format, or inference behavior changed; 561 PASS 0 FAIL maintained; tagged milestone-125-tensor-exec-plan.
- Milestone 126: FPGA prototype feasibility notes — `docs/fpga_feasibility.md` added (~430 lines): research/advisory documentation only; no RTL, no board commitment, no kernel driver, no real PCIe; §1 executive summary: DEFER — register map, command schema, and fabric route schema are not frozen; 2 of 11 gate criteria met; continue software/userspace emulator path; §2 prototype purpose: PCIe enumeration, BAR0/MMIO, AXI-Lite register block, doorbell/completion FSM, DMA descriptor validation, counter/trace registers, optional fabric route replay; not tensor inference; §3 minimum viable FPGA design: PCIe endpoint IP, AXI-Lite BAR0 slave (M104 offsets), command queue BRAM, completion queue BRAM, tile capability registers, descriptor validator FSM, counter/trace block; DMA engine and interrupts deferred; §4 M104 register map to AXI-Lite mapping: BAR0 offset table (6 regions), RO/RW/RW1C/WO semantics, 64-bit pair write/read order, doorbell write strobe behavior, counter snapshot latch, reset behavior; §5 PCIe IP options (Xilinx/AMD Vivado hard IP + XDMA; Intel/Altera Quartus hard IP + DFL framework; LitePCIe open-source; AXI-Lite bridge; DMA deferred; VFIO preferred host path); §6 host software options (mmap emulator M121, UIO, VFIO, custom pci_driver; VFIO preferred; kernel driver deferred); §7 board considerations (Gen2×4 preferred; ≥4 Mb BRAM; 20K–50K LUTs; low/mid-range device sufficient; DDR/HBM deferred; cost classes noted as approximate/uncertain); §8 qualitative resource estimates table (BAR0=low, cmdq=low, descriptor FSM=low/moderate, counter/trace=low, DMA=moderate/high, fabric=moderate/high, tensor math=high, q4/q8 matmul=high, full tile=very high); §9 risks table (12 risks: register map churn=HIGH, command schema churn=HIGH, fabric model instability=HIGH, hardware effort distraction=HIGH, timing closure=medium/high, DMA correctness=high; others low/medium); §10 FPGA gate criteria table (11 criteria; M121+M122+M123+M124+M125 complete, 6 criteria not yet including register map freeze, command schema freeze, route schema freeze, host path selection, M131 regression suite, M132 go/no-go); §11 recommendation (NOT YET; continue M127–M131; revisit at M132); §12 future milestone proposals M127–M132; §13 non-goals; no C, Makefile, binary format, or inference behavior changed; 561 PASS 0 FAIL maintained; tagged milestone-126-fpga-feasibility.
- Milestone 127: Phase 3 prototype BOM and board options review — `docs/phase3_bom_board_options.md` added (~380 lines): research/planning documentation only; no board selected or purchased; §1 executive summary (hardware not ready for purchase; 5 of 12 gate conditions met; continue userspace emulator path; revisit at M132/M133); §2 BOM categories (FPGA/PCIe dev board, host workstation/PCIe slot, power/cooling, cabling/adapters, debug/JTAG tools, model artifact storage, toolchain licensing, spare parts/risk buffer); §3 six board option classes with attribute tables for each: no-hardware userspace emulator (recommended current path), low-cost FPGA dev board (Artix-7/Cyclone V, ~USD 100–500, BAR0 + cmdq + descriptor validation), midrange FPGA PCIe board (Kintex-7/UltraScale/Arria-10, ~USD 500–3000, + DMA + basic q8 matmul), high-end FPGA/HBM board (UltraScale+HBM/Versal/Stratix-10, ~USD 3000–15000+, + sustained tensor inference), embedded SoC FPGA (Zynq/MPSoC, ~USD 200–1500, ARM+FPGA alternate path), custom PCIe carrier board (USD 50K–500K+ NRE, very high risk, deferred indefinitely); §4 minimum viable FPGA capabilities table (PCIe Gen2 hard IP, ≥64 KiB BAR0, AXI-Lite, ≥4 Mb BRAM, JTAG, Linux host compat, free-tier toolchain); §5 host requirements (x86_64 Linux ≥5.10, ≥16 GiB RAM, PCIe x4 Gen2+ with IOMMU, UIO→VFIO→pci_driver path, 3090-class server compatibility note); §6 cost/risk classes table (zero/low/midrange/high/very-high); §7 decision matrix table (6 board classes × 11 criteria: BAR0, cmdq, DMA descriptor, real PCIe, future DMA, fabric counter, tensor math, toolchain difficulty, schedule risk, cost risk); §8 recommended near-term path (continue no-hardware emulator; don't purchase yet; shortlist at M132; gate at M133); §9 hardware purchase gate table (12 conditions; 5 complete M121–M125, 7 not yet); §10 risks table (12 risks: register map churn HIGH, command schema churn HIGH, scope creep into tensor math HIGH, toolchain lock-in medium, PCIe IP friction medium/high, BRAM insufficiency medium); §11 future milestone proposals M128–M133; §12 non-goals; no C, Makefile, binary format, or inference behavior changed; 561 PASS 0 FAIL maintained; tagged milestone-127-phase3-bom-review.
- Milestone 128: Execution-plan validator — `compiler/validate_tensor_execution_plan.py` added (~460 lines): validates M125 execution-plan JSON for structural correctness, phase ordering, command integrity, dependency consistency, fabric/reduction requirements, and buffer field correctness; does not execute inference, change runtime behavior, access real PCIe/MMIO, or implement a kernel driver; 16 recognised execution phases (DEVICE_PROBE through RESET); 16 recognised command types (LOAD_TENSOR_TILE through QUERY_COUNTERS); 14 validation rules with coded error IDs (E01–E20): E01 header fields (version, model_id, session_id, tile_count, command_count), E02 unique plan_command_id, E03 valid tile_id, E04 known execution_phase, E05 known command_type, E06 tensor_dependencies required for matmul/rmsnorm/load/validate, E07 buffer descriptors (region_type, byte_size, dtype; q4 metadata warning/strict-error), E08 expected_status recognised, E09 trace_flags non-negative, E10 dependency_fence_id forward-only and acyclic DAG, E11 required_routes/reductions are lists, E12 FABRIC_SEND dst_tile valid, E13 FABRIC_REDUCE reduction_behavior required, E20 phase ordering partial-order constraints; exit 0=pass, 1=fail, 2=parse error; `--strict` promotes warnings to errors; `--report-json` writes JSON summary with status/warnings/failures/phase_count/command_count/route_ref_count/buffer_ref_count; 1 valid tiny fixture (exec_plan_valid_tiny.json; 1-tile, 1-layer, 6 commands, 6 phases: LOAD_TENSOR_TILE/VALIDATE_TENSOR_TILE/PREFILL_SETUP/PREFILL_EXECUTION_PLAN/KV_APPEND/QUERY_COUNTERS); 9 invalid fixtures: exec_plan_missing_header.json (E01), exec_plan_duplicate_cmd_id.json (E02), exec_plan_unknown_phase.json (E04), exec_plan_future_dep.json (E10 future dep), exec_plan_cyclic_dep.json (E10 forward check catches cycle), exec_plan_missing_tensor.json (E06 EXEC_MATMUL no tensor dep), exec_plan_missing_reduction.json (E13 FABRIC_REDUCE no reduction_behavior), exec_plan_invalid_tile.json (E03 tile_id >= tile_count), exec_plan_q4_bad_meta.json (W07 / E07 in strict mode); `docs/tensor_execution_plan.md` §12 added (invocation, rules table, fixtures table, non-goals); `docs/aimu_pcie_prototype_review.md` M128 row added; no C, Makefile, binary format, or inference behavior changed; 561 PASS 0 FAIL maintained; tagged milestone-128-exec-plan-validator.
- Milestone 129: Execution-plan-to-command-plan mapper — `compiler/map_execution_plan_to_commands.py` added (~430 lines): reads M125 execution-plan JSON and emits M109-compatible AIMU command-plan JSON suitable for replay through existing command-plan tooling (M113, M122); does not execute inference, change runtime behavior, access real PCIe/MMIO, or implement a kernel driver; mapping rules for all 16 execution phases and 16 command types; advisory phases (DEVICE_PROBE, TILE_ENUMERATION, MEMORY_ALLOCATE, PREFILL_SETUP) emit advisory NOP/QUERY_COUNTERS/TILE_BARRIER commands with descriptive notes; EXEC_* types (EXEC_MATMUL, EXEC_RMSNORM, EXEC_ROPE, EXEC_ATTENTION, EXEC_SWIGLU, EXEC_SOFTMAX, EXEC_RESIDUAL) pass through with `expected_status=ATT1_AIMU_ERR_UNSUPPORTED_OP` in non-strict mode or raise MappingError in `--strict` mode; passthrough types (LOAD_TENSOR_TILE, VALIDATE_TENSOR, KV_APPEND, KV_READ, FABRIC_SEND, FABRIC_REDUCE, TILE_BARRIER, TRACE_SNAPSHOT, QUERY_COUNTERS) emit with STATUS_OK and resolved src/dst descriptors; RESET → RESET_TILE; CLEANUP+QUERY_COUNTERS → QUERY_COUNTERS; exit 0=success, 1=mapping error, 2=parse error; `--strict` rejects EXEC_* types; `--plan-json` writes output command plan JSON; `--model-id`/`--session-id` override plan values; output format is M109 command-plan v1 with header, commands array (21 fields per record), summary, and warnings array; reference fixture `compiler/fixtures/exec_plan_mapped_cmd_plan.json` produced from exec_plan_valid_tiny.json (6 commands: LOAD_TENSOR_TILE, VALIDATE_TENSOR, TILE_BARRIER, EXEC_MATMUL/UNSUPPORTED, KV_APPEND, QUERY_COUNTERS); `docs/tensor_execution_plan.md` §13 added (invocation, mapping table, fixture, non-goals); `docs/aimu_pcie_prototype_review.md` M129 row added; no C, Makefile, binary format, or inference behavior changed; 561 PASS 0 FAIL maintained; tagged milestone-129-exec-plan-mapper.
- Milestone 130: Simulated AIMU EXEC command replay — `include/att1_aimu_exec.h`, `src/aimu_exec.c`, `tests/test_aimu_exec.c` added; `att1_aimu_exec_ctx` wraps a non-owning `att1_aimu_device*`; `att1_aimu_exec_dispatch` replays any `att1_aimu_cmd` without tensor math; validates tile_id range against `device->tile_count` when device is non-NULL, tensor_id ≠ 0 for EXEC_MATMUL, dtype bitmask via `dtype_to_bit()` (0=f32, 1=q8, 2=q4), and op bitmask from `tile->supported_ops`; `att1_aimu_exec_counters` tracks 17 uint64_t fields: exec_commands_seen, exec_commands_completed, exec_commands_failed, exec_unsupported, matmul_count, rmsnorm_count, rope_count, attention_count, ffn_count, kv_append_count, kv_read_count, fabric_send_count, fabric_reduce_count, barrier_count, trace_snapshot_count, bytes_read_estimate, bytes_written_estimate; `att1_aimu_exec_ctx_reset_counters` zeroes all counters; NULL device → ATT1_AIMU_OP_ALL / ATT1_AIMU_DTYPE_ALL; `ATT1_AIMU_EXEC_CTX_MAGIC = 0xA771E5EC`; LOAD_TENSOR_TILE updates bytes_written_estimate from output_buf_bytes; TILE_BARRIER increments barrier_count, TRACE_SNAPSHOT increments trace_snapshot_count; unknown command type → ATT1_AIMU_ERR_INVALID_COMMAND; 94 new tests across 14 test functions in test_aimu_exec; `docs/tensor_execution_plan.md` §14 added (invocation, dispatch table, counter struct, non-goals); `docs/aimu_pcie_prototype_review.md` M130 row added; Makefile updated (aimu_exec.c to COMMON_SRCS, aimu_exec to TEST_NAMES); no inference/backend/CUDA/tokenizer/binary-format changes; 655 PASS 0 FAIL; tagged milestone-130-aimu-exec-replay.
- Milestone 131: Userspace MMIO emulator regression suite — `tests/test_aimu_mmio_regression.c` added; 16 test functions, no new production source; covers 7 regression categories: (1) device probe — DEVICE_ID/REGISTER_MAP_VERSION/TILE_COUNT/GLOBAL_STATUS/per-tile metadata via `att1_aimu_host`; (2) register behavior — INTERRUPT_ENABLE RW round-trip, out-of-range/unaligned/RO-write/read64-unaligned error paths (INVALID_ARG or UNSUPPORTED), reserved-offset sentinel 0xDEADBEEF, discarded write; (3) RESET_CONTROL — RESET_COUNTERS zeroes CNT_CMD_ISSUED_LO, WO register reads 0; (4) cmdq/doorbell — submit NOP, ring doorbell, drain, read completion, doorbell_write_count, FIFO order (3 commands, monotonic command_ids), QUEUE_FULL on depth-4 ring, completions == submitted; (5) DMA descriptor — valid H2D validates and submits OK (permissive mode), dma_submitted/completed/bytes_host_to_device counters, zero-length/unaligned-host-addr/unaligned-device-addr/out-of-range failures, dma_failed increment, 4 GiB tile_memory_bytes no-alloc (OK or INVALID_ARG, no crash); (6) EXEC replay (M130) — all 11 ops (5 EXEC_* / 2 KV / 2 FABRIC / barrier / trace_snapshot) with full-capability device via `att1_aimu_exec_dispatch`, per-op counters, exec_commands_seen/completed/failed/unsupported fields, restricted-device EXEC_MATMUL → UNSUPPORTED_OP via exec and cmdq dispatchers; (7) replay integration — 6-command M129 plan through `att1_aimu_host`, TILE_BARRIER/QUERY_COUNTERS OK, EXEC_MATMUL UNSUPPORTED_OP, 6 completions, summary commands_submitted>=6/fence_value>0, tile_id=99 rejected INVALID_ARG; (8) trace snapshot determinism — two identical reset-separated 3-command replays, cmdq counter snapshots identical, trace_version/tile_count/commands_submitted verified; compile-time CUDA guard; `docs/aimu_pcie_prototype_review.md` M131 row added; Makefile updated (aimu_mmio_regression to TEST_NAMES, no new COMMON_SRCS); no inference/backend/CUDA/tokenizer/binary-format changes; 781 PASS 0 FAIL; tagged milestone-131-mmio-regression.
- Milestone 132: Integrated execution/replay pipeline regression report — `compiler/run_execution_replay_pipeline.py` added; 6-stage end-to-end pipeline starting from M125 tensor execution plan: (1) validate execution plan (M128: `validate_tensor_execution_plan.py`), (2) map to command plan (M129: `map_execution_plan_to_commands.py`), (3) MMIO command replay (M122: `replay_command_plan_via_mmio.py` + `build/att1-aimu-mmio-replay` binary), (4) map to fabric routes (M117: `map_commands_to_fabric_routes.py`), (5) validate fabric routes (M116: `validate_fabric_routes.py`), (6) replay fabric routes + bandwidth sim (M123/M118: `replay_fabric_routes.py`); PIPELINE_VERSION=132; JSON report with 21 fields including `execution_plan_validation_status`, `command_plan_status`, `mmio_replay_status`, `fabric_route_status`, `fabric_replay_status`, `fabric_simulation_status`, `exec_commands_seen`, `required_fabric_gib_sec`, `fabric_status`, `final_status`; `_normalise("UNKNOWN")→"warn"` consistent with M119 `_STATUS_RANK`; binary-absent check before stage 3 (marks warn, continues); `--strict` propagates to all sub-tools; `--workdir` preserves numbered intermediate JSONs 01–06; `--tiles`/`--tile-memory-mib`/`--kv-memory-mib` forwarded to MMIO emulator; exit 0=pass/warn, 1=fail, 2=parse, 3=invalid flag; `compiler/test_execution_replay_pipeline.py` added: 14 test functions, 16 assertions covering valid-tiny pass/warn, all-keys present, pipeline_version=132, stage statuses, count types, malformed-plan fail, invalid-tile fail, strict-mode exit, determinism, no-CUDA, invalid-flags, binary-absent skip, exec_commands_seen, workdir intermediates; `docs/aimu_pcie_prototype_review.md` M132 row added; no C/Makefile/inference behavior changed; 16 PASS 0 FAIL (Python); 781 PASS 0 FAIL (C); tagged milestone-132-execution-replay-pipeline.
- Milestone 133: Deterministic golden regression baselines — `compiler/check_golden_regressions.py` added; 12-check golden regression suite (`_Check` dataclass with `id`, `label`, `tool`, `make_args`, `output`, `golden`, `extract`, `skip_if`); `_CHECKS` registry covering all ATT-1 AIMU planning and replay pipeline tools: g01 placement report validation, g02 placement advisory, g03 placement scenario generation, g04 command plan from placement, g05 command plan replay (M113; skip_if att1-aimu-replay absent), g06 fabric route report, g07 fabric bandwidth simulation, g08 fabric route replay, g09 execution-plan validation, g10 execution-plan to commands, g11 integrated execution/replay pipeline (M132; skip_if att1-aimu-mmio-replay absent), g99 malformed exec plan negative golden; chained workdir: g04 output → g05+g06, g06 output → g07+g08; `--check` (default) compares live tool output against stable fields extracted by per-check extract functions; `--update-golden` regenerates golden JSON files; `--fixture-dir` override; extract functions exclude volatile fields (timestamps, paths, elapsed times) and retain semantic fields (status, counts, command types, bytes); `compiler/fixtures/golden/` directory with 12 checked-in JSON baseline files; negative golden g99 detects future regression where malformed exec plan is silently accepted; exit 0=all pass, 1=one or more fail; 12 PASS 0 FAIL (Python golden check); 781 PASS 0 FAIL (C) maintained; `docs/aimu_pcie_prototype_review.md` M133 row added; tagged milestone-133-golden-regressions.

## Next Prompt for Codex

Implement Milestone 134 only.

## Known Risks

- OPERATION_LOG ordering drift.
- CLI/docs drift.
- CUDA accidentally required by default build.
- Silent backend fallback.
- q4 tolerance/token divergence not documented.
- q4 metadata/packing mismatch.
- CUDA q4 kernel mismatch vs CPU q4.
- Hostile-input validation gaps.
- Public model artifacts accidentally committed.
- Python cache artifacts accidentally tracked.
- Tokenizer mismatch / token ID drift.
- ATT-1 format drift without versioning.

## Standard Post-Milestone Checklist

- `make clean && make && make test`
- `make clean && make CUDA=1 && make test CUDA=1` when CUDA touched. CUDA validation rule:
For milestones touching CUDA code, CUDA benchmarks, CUDA reports, or CUDA backend behavior, the milestone is not complete until Rick manually validates on the RTX 3090 host with:

make clean && make CUDA=1 && make test CUDA=1

and any milestone-specific CUDA smoke commands.

Until that signoff, the milestone status is:
"CPU-validated; CUDA signoff pending."
- `git diff --stat`
- `git diff`
- `git ls-files` cache check:
  `git ls-files | grep -E '(__pycache__|\.pyc$|\.pyo$)' || echo "No tracked Python cache artifacts"`
- Commit with a milestone-specific message.
- Tag the milestone.
- Push.
- Update `docs/OPERATION_LOG.md`.
